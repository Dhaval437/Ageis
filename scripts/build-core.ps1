# Builds the Python sidecar with PyInstaller (onedir, per REMEMBER.md § 4) into
# core/dist/aegis-core, where electron-builder.yml picks it up as extraResources.
#
# Scaffold only — implemented in P7.
#Requires -Version 5.1
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

Write-Host 'build-core.ps1 is a placeholder; implemented in P7.'
