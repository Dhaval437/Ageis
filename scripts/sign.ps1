# Authenticode-signs the sidecar and the installer.
#
# Start P7-09 (antivirus false-positive submissions) in week one — it takes weeks
# of calendar time and blocks launch (REMEMBER.md § 7).
#
# Scaffold only — implemented in P7.
#Requires -Version 5.1
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

Write-Host 'sign.ps1 is a placeholder; implemented in P7.'
