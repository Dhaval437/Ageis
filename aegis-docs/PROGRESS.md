# PROGRESS.md

> **The single source of truth for what is done, what is next, and who is doing it.**
> Every agent session **must** update this file before it ends. An unrecorded change is a lost change.
>
> Task IDs are permanent. Never renumber. New work gets the next free number in its phase (e.g. `P3-11`).

---

## 0. How to use this file

**At the start of a session**

1. Read `REMEMBER.md`, then this file's §1 and §2.
2. Pick the topmost `TODO` task whose blockers are all `DONE`.
3. Set it to `WIP` with your session date. Only **one** task may be `WIP` at a time per developer.

**At the end of a session (mandatory)**

1. Set the task to `DONE`, `BLOCKED`, or back to `TODO` with a note.
2. Append a line to §6 Session Log.
3. If you made an architectural decision, add it to `REMEMBER.md § Decision Log` — not here.
4. If you discovered new work, add it as a new task with the next free ID.

**Status values:** `TODO` · `WIP` · `BLOCKED` · `REVIEW` · `DONE` · `CUT`
A task is only `DONE` when it passes the corresponding gate in `REVIEW.md`.

---

## 1. Snapshot

|                              |                                                                                                                       |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| **Current phase**      | P2 — Perception                                                                                                      |
| **Current task**       | P3-07 is in REVIEW (one hand run of the built app left). Next: P3-16 — the preemption test flake (topmost unblocked). Also unblocked: `P3-14` (preemption UX), `P3-19` (approval window), `P3-18` (Rules screen), `P3-17` (scope routes + UI, security-gated), `P3-15` (MAIN's own modifier release — the kill switch and watchdog both need it) and `P3-16` (the preemption tests that failed once in a full run). P2-12 stays BLOCKED on a packaging decision |
| **Last session**       | 2026-09-26 — P3-15 MAIN's modifier release |
| **Overall**            | 54 / 114 tasks                                                                                                        |
| **Ship target for v1** | Windows installer, Standard autonomy, fs + input + shell + browser tools                                              |

### Phase progress

| Phase | Name                     | Tasks | Done | Status | Exit gate                                                         |
| ----- | ------------------------ | ----- | ---- | ------ | ----------------------------------------------------------------- |
| P0    | Foundations & plumbing   | 18    | 18   | ✅     | App launches, core handshake works, one round-trip                |
| P1    | Model layer              | 10    | 10   | ✅     | Chat with any of 3 providers; keys stored in DPAPI                |
| P2    | Perception               | 14    | 13   | 🟨     | Agent can describe the screen and list clickable elements         |
| P3    | Actuation + safety spine | 19    | 13   | 🟨     | Agent clicks correctly; kill switch and preemption both < targets |
| P4    | The agent loop           | 12    | 0    | ⬜     | 8 of 10 benchmark tasks complete unattended                       |
| P5    | Tool families            | 13    | 0    | ⬜     | fs / shell / browser all behind Guardian                          |
| P6    | Recovery, undo, audit    | 10    | 0    | ⬜     | Every destructive op is undoable; log chain verifies              |
| P7    | Packaging & installer    | 10    | 0    | ⬜     | Signed`Setup.exe` installs and auto-updates on a clean VM       |
| P8    | Hardening & polish       | 8     | 0    | ⬜     | Threat-model tests green; a11y pass; perf targets met             |

---

## 2. P0 — Foundations & plumbing

| ID    | Task                                                                                                                                                                                                                                       | Blocked by   | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-01 | Monorepo scaffold: pnpm workspaces, turbo, tsconfig base, ruff/mypy/eslint/prettier configs,`.editorconfig`                                                                                                                              | —           | DONE   | 2026-09-07. Layout per`ARCHITECTURE.md § 4`; docs stayed in `aegis-docs/`. `pnpm build` + `pytest` green                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| P0-02 | Electron MAIN skeleton: window, custom titlebar, tray, single-instance lock                                                                                                                                                                | P0-01        | DONE   | 2026-09-07. Frameless 1100×720 (min 880×600), tray with status/stop/show-hide/quit, single-instance lock verified. Renderer draws the titlebar in P0-03, so the window is currently drag-less; window controls need P0-04                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| P0-03 | Renderer skeleton: Vite + React + Tailwind + shadcn, dark tokens from`UI.md § 2`                                                                                                                                                        | P0-01        | DONE   | 2026-09-09. Both palettes wired (light derived — see Decision Log), stock Tailwind scales cleared so off-token classes don't compile, shadcn foundation (`components.json`, `cn`, `Button`), `AppShell` to the `UI.md § 4` layout. Vitest + RTL added; 35 renderer tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| P0-04 | Preload bridge with the exact 6-namespace surface                                                                                                                                                                                          | P0-02        | DONE   | 2026-09-09. All six namespaces, one named channel per method, no generic`invoke`. Contract in `packages/shared/src/bridge.ts`; validation + policy in `main/bridge-handlers.ts` (electron-free); Electron wiring, sender check and disposal in `main/ipc.ts`. `system.openPath`/`revealInExplorer` gated by `main/path-grants.ts`. Unbuilt subsystems answer `unavailable`. Titlebar minimise/close now live                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| P0-05 | Python core skeleton: FastAPI app,`/v1/health`, structured logging to file + stdout                                                                                                                                                      | P0-01        | DONE   | 2026-09-09.`server/app.py` (`create_app()`), `server/routes.py` (`/v1/health`), `server/schemas.py` (`HealthResponse`), `logging_setup.py` (JSON lines → rotating file + **stderr**, not stdout — see Decision Log), `__main__.py` serves it on loopback. Docs/OpenAPI off. Handshake, auth and peer-PID check are P0-06                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P0-06 | **Startup handshake**: ephemeral port, token over stdin pipe, stdout JSON line, peer-PID check                                                                                                                                       | P0-02, P0-05 | DONE   | 2026-09-09. Steps 1–3 and 5 of`ARCHITECTURE.md § 3.1`. MAIN: `main/handshake.ts` (`startCore` — 256-bit token over the stdin pipe, one JSON line read back, child killed on every failure path) and `main/core-gateway.ts` (the only HTTP client; adds origin, port, `/v1` and the bearer token, never an `Origin`). Core: `server/handshake.py` (`read_token`/`bind_loopback`/`announce`) and `server/auth.py` (`SessionAuthMiddleware` — no `Origin`, constant-time token, peer-PID check via psutil). Steps 4 and 6 — health check, 3-strike respawn, kill-on-exit — are P0-07, so this is **not wired into `index.ts` yet**: a spawn without a lifecycle is invariant 14's failure mode                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| P0-07 | Supervisor: spawn, health-check, 3-strike respawn, kill core when MAIN exits                                                                                                                                                               | P0-06        | DONE   | 2026-09-10.`main/supervisor.ts` (`createSupervisor` — health check on `/health`, restart on an unexpected exit, 3 attempts inside a 60 s window then `unavailable`; `resolveCoreLaunch` picks the packaged `aegis-core.exe` or the dev venv interpreter, `AEGIS_CORE_COMMAND` overrides both) and the wiring in `index.ts` (`rest.core` is the live gateway or `null`; `before-quit` stops the core). Core: `aegis_core/parent_watch.py` watches the supervising PID and stops uvicorn when it dies, so a MAIN killed from Task Manager still takes the core with it. Measured: real app, MAIN force-killed → core gone in **0.20 s**; graceful quit → **0.33 s**; parent watch in isolation (supervisor killed, pipes untouched) → **0.71 s**. Budget is 2 s. **Not** here: the 1/s watchdog (P3-07) and the Engine-unavailable screen (P0-17)                                                                                                                                                                                                                                                                                                            |
| P0-08 | WebSocket event hub +`seq` replay from memory (SQLite replay lands in P6)                                                                                                                                                                | P0-05        | DONE   | 2026-09-13.`server/hub.py` (`EventHub` — thread-safe `publish`, 2048-event replay buffer, per-subscriber queue of 512 then disconnect with `4429`), `WS /v1/stream` in `server/routes.py`, `StreamEvent` + `EventType` in `server/schemas.py`, hub on `app.state.hub`. `since` is exact or refused (`4410`), never replayed with a hole. Close codes in `ARCHITECTURE.md § 9.2`. **Nothing publishes yet**, and MAIN does not connect yet (P0-09)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| P0-09 | Renderer event-stream client + Zustand store; UI is a pure function of the stream                                                                                                                                                          | P0-08, P0-03 | DONE   | 2026-09-14. MAIN:`main/core-stream.ts` holds the one WebSocket (`ws`, token in MAIN, no `Origin`, 4 MiB frame cap). It reconnects to the same core with `?since=` and backoff, sends `reset` for a new core, for `4410`/`4400`/`1003` and on every page load, and reports `connecting`/`live`/`down`/`unavailable`. `supervisor.onSession` tells it which core is live. Contract: `CoreStreamMessage` in `shared/bridge.ts`, on the existing `aegis:core:event` channel (no new bridge member). Renderer: `lib/stream-event.ts` validates every message, `stores/stream.ts` (`reduceStream`, bounded to 1000 events), `lib/stream-client.ts` subscribes before first render, and the titlebar shows the engine note. `StreamEvent`/`EVENT_TYPES` are **hand-seeded** in `shared/api.ts` for P0-11 to replace. `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-14`                                                                                                                                                                                                                                                                 |
| P0-10 | SQLite bootstrap + migration runner + the schema from`ARCHITECTURE.md § 7`                                                                                                                                                              | P0-05        | DONE   | 2026-09-14.`storage/db.py`: `connect()` (WAL verified, `synchronous=FULL`, foreign keys on, `trusted_schema=OFF`, 5 s busy timeout, SQLite ≥ 3.38 required), `migrate()` (`PRAGMA user_version`; each migration and its version bump in one transaction; a DB newer than the build is refused; a misnumbered list is refused), `bootstrap()` run by `__main__.py` after the token and **before** the handshake line, exit code 4 on failure. `storage/migrations/m0001_initial.py`: all eight § 7 tables, `STRICT`, locked vocabularies `CHECK`ed, JSON columns `json_valid`, `audit` append-only by trigger. No connection is held yet — the first reader/writer decides how connections are shared. A corrupt DB currently makes the core fail to start (→ supervisor `unavailable`); recovering from that is P6-10                                                                                                                                                                                                                                                                                                                                                    |
| P0-11 | Pydantic→TS type generation script wired into`pnpm dev`                                                                                                                                                                                 | P0-05, P0-03 | DONE   | 2026-09-14.`core/aegis_core/server/typegen.py` introspects `server/schemas.py`: each `BaseModel` defined there → `readonly` interface with JSDoc, each module-level `Literal` alias → `const NAME_S` tuple + derived type, serialised shape, unknown annotations raise. `scripts/gen-types.ts` runs it with the venv interpreter (60 s timeout), prettier-formats, writes `packages/shared/src/api.ts`. `pnpm dev` regenerates first; `pnpm test` starts with `pnpm gen:types:check` (exit 1 if stale — there is no CI yet, see P0-18). The hand-seeded `EVENT_TYPES`/`EventType`/`StreamEvent` and the hand-written `RiskTier` are now generated with the same names and shapes; `HealthResponse` is new in TS. `RiskTier` moved into `schemas.py`; the unused `SHARED_PACKAGE_NAME` placeholder is gone                                                                                                                                                                                                                                                                                                                                                             |
| P0-12 | Fix the root`clean` script: `rimraf node_modules` deletes the rimraf it is running from, so it always exits 1 half-done                                                                                                                | —           | DONE   | 2026-09-14.`scripts/clean.ts` (Node built-ins only, run on type stripping) replaces `rimraf node_modules`: it removes the root **and every workspace package's** `node_modules`, and removes pnpm's junctions without following them into package source. `pnpm clean` = `turbo run clean && node scripts/clean.ts`, exits 0. The now-unused root `rimraf` devDependency is gone (packages keep theirs for `dist`/`.turbo`). 4 `node:test` tests in `scripts/clean.test.ts`, incl. a real junction into a source folder and parity with `pnpm-workspace.yaml`, run by `pnpm test` via `pnpm scripts:test`. `pnpm install` is still needed after a clean, as intended. A second `pnpm clean` with nothing installed fails at `turbo`, because there is no turbo to run                                                                                                                                                                                                                                                                                                                                                                                                    |
| P0-13 | Storybook for the renderer, with the four required stories per component                                                                                                                                                                   | P0-03        | DONE   | 2026-09-15. Storybook 10 (`@storybook/react-vite`, pinned) on the app's `vite.config.ts`, telemetry and update checks off. `.storybook/preview.tsx`: theme toolbar sets `data-theme`, every story starts from `INITIAL_STREAM`, stub `window.aegis` (`story-bridge.ts`) answers `unavailable`. `.storybook/states.ts`: the five required states + the `UI.md § 12` inventory. Stories for `AppShell` (Default/Loading/Error/LongestContent at 880×600/Reconnecting) and `Button`. `tests/stories.test.tsx` (30 tests, in `pnpm test`): every component has a stories file, every required state exists or is declared not applicable with a reason, `Error` never skipped for an inventory component, every story renders, and AppShell's stories really reach the store. Scripts `storybook` / `build-storybook`; `storybook-static/` ignored                                                                                                                                                                                                                                                                                                                           |
| P0-14 | Bundle Inter + JetBrains Mono as local`woff2` and self-host them                                                                                                                                                                         | P0-03        | DONE   | 2026-09-15.`@fontsource-variable/inter` + `/jetbrains-mono` 5.3.0 (exact-pinned) imported in `index.css`, and the stacks lead with `Inter Variable` / `JetBrains Mono Variable`. Preflight's `--default-font-family`/`--default-mono-font-family` point at the tokens, so a bare `<code>` gets the mono face. `vite.config.ts` never inlines `.woff2`, because an inlined subset was refused by the CSP. The CSP is unchanged. `tests/fonts.test.ts` (12 tests): package CSS uses bundled relative woff2 files only, a real in-memory `vite build` emits every face as a file with no `data:font`, and `font-src` resolves to `'self'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| P0-15 | Maximise/restore: add`window.maximize` to the bridge surface, then wire the `□` button                                                                                                                                                | P0-04        | DONE   | 2026-09-16.`§ 9.3`'s `window` namespace gains **two** members: `maximize()` (a toggle — MAIN decides which way it flips) and `onMaximizedChange(listener)`, because double-clicking the drag region, `Win`+`↑` and Aero snap all maximise a frameless window and a button that tracked only its own clicks would show the wrong icon (Decision Log). MAIN pushes on Electron's `maximize`/`unmaximize` and on `did-finish-load`. The toggle decision lives in the electron-free `bridge-handlers.ts` (`WindowService.isMaximized`/`setMaximized`) so it has a test; `ipc.ts` supplies two Electron calls and no logic. Renderer: `stores/window.ts` + `lib/window-client.ts`, connected in `main.tsx` before the first render; the control is *Maximise* (`Square`) / *Restore* (`Copy`), label **and** icon. `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-16`                                                                                                                                                                                                                                                              |
| P0-17 | **Engine unavailable** screen (`RECOVERY.md § 4`): Restart engine / Open logs / Copy report, driven by the supervisor's `unavailable` state                                                                                     | P0-07, P0-09 | DONE   | 2026-09-16.`components/EngineUnavailable.tsx` fills the conversation panel while the stream reports `unavailable`, with `§ 4`'s copy verbatim and its three buttons. The surface gained **two** members (Decision Log): `core.restart()` — the supervisor kills what is there, **clears the attempt window** and starts over, and MAIN throws unless the core ends up `running` — and `app.copyDiagnosticReport()`, which builds and redacts the report in MAIN and writes the clipboard there, so log text never enters the renderer. `Open logs` needed nothing new, but `app.logsPath()` now answers `%LOCALAPPDATA%\Aegis\logs` (`main/paths.ts`), the folder the **core** writes to, which MAIN creates and grants. `main/diagnostics.ts` is the redactor: home paths → `%USERPROFILE%\…`, key/token/auth fields and 32+ char blobs → `[redacted]`, lines capped, last 200 lines tailed. `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-16`                                                                                                                                                                                   |
| P0-16 | Narrow the peer-PID check so a job the**core** spawns cannot call the core's own API                                                                                                                                                 | P0-06, P5-08 | DONE   | 2026-09-16, done**ahead of** its P5-08 blocker at the user's request — the rule is self-contained and the tracker note only required it to land before P5-08 ships. The peer must now be the supervising process **itself**, identified by PID *and* creation time (`SessionAuth.is_the_supervisor`), not a member of its child chain: the core is a descendant of MAIN, so every job the core spawns was too. `is_supervised_by` and its ancestry walk are gone; `identities_match` reuses `parent_watch.process_identity`, and `AccessDenied` falls back to PID equality rather than locking MAIN out. Nothing needed the wider rule — MAIN is the only client and opens every socket in its own process. 7 new tests, incl. a real-subprocess one that asserts the job really is inside MAIN's chain before asserting it is refused. `ARCHITECTURE.md § 3.1` step 5 and `§ 8.6` updated. **Proved against a real core both ways:** with the new rule MAIN gets 200 and a job below the core with the *same valid token* gets 401; with the old rule stashed back in, that job got 200. `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-16` |
| P0-18 | CI workflow (GitHub Actions,`windows-latest`): `pnpm install --frozen-lockfile`, core venv + `pip install -e .[dev]`, then `pnpm build`, `pnpm lint`, `pnpm typecheck`, `pnpm py:lint`, `pnpm py:typecheck`, `pnpm test` | P0-11        | DONE   | 2026-09-16.`.github/workflows/ci.yml` — one `windows-latest` job on push and PR to `main`, `permissions: contents: read`, `concurrency` cancelling superseded runs, 30 min timeout. Node 22.x (`engines.node`) with the pnpm store cached, Python 3.11 (the target runtime), venv at **`core/.venv`** because `py:*` and `scripts/gen-types.ts` hard-code that path. No separate `py:test` step: `pnpm test` already runs `gen:types:check`, every workspace suite, `scripts:test` and `pytest`, so nothing can be enforced in CI and skippable locally. `scripts/ci-workflow.test.ts` (7 `node:test` tests, added to `pnpm scripts:test`) fails if the workflow drifts from the repo. **Not verified on a runner** — GitHub Actions cannot be executed from here; the first push proves it                                                                                                                                                                                                                                                                                                                                                                    |

**Gate:** typing in the composer sends a request to the core and a streamed echo renders in the timeline. Kill MAIN → no orphan process.

> Closed at P1-10 (2026-09-20). Kill MAIN → no orphan has been verified every session; the round-trip half is now met too — the Models screen sends real requests to the core over `core.request`, a key saved there reaches Windows Credential Manager, and *Test* streams a real answer back from a provider. The **composer** itself is still disabled: it needs a task, which is P4. Nothing was added to the preload surface to do any of it (`§ 9.3` is unchanged since P0-17).

---

## 3. P1 — Model layer

| ID    | Task                                                                                   | Blocked by   | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | -------------------------------------------------------------------------------------- | ------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1-01 | `ModelProvider` protocol + `ChatRequest`/`ChatDelta`/`Capabilities` schemas    | P0-05        | DONE   | 2026-09-16.`models/schemas.py` (the shapes) + `models/provider.py` (the interface). `ChatRequest` carries the model id, a tagged `ChatMessage` tuple (system / user / assistant / tool, text and image parts), the `ToolDef`s on offer, and a `timeout_s` with a default, because `REVIEW.md § 2` says every external call has one. `ChatDelta` is a tagged union — `text` / `tool_call` / `usage` / `done` — and a stream ends in exactly one `DoneDelta`; a tool call is only emitted once its arguments parse, so nothing downstream ever sees half a call. `Capabilities` answers **per model**, not per provider, and prices input and output separately for `P1-09`. Everything is frozen, `extra="forbid"`, tuples not lists. **Three security choices, all tested:** `ChatRequest` has no field a key could sit in (asserted by field name); `ImagePart.data` is `repr=False`, so an observation cannot land in a log line; `ProviderError` carries a provider, a status and a short reason — never a response body. The error hierarchy ships with the protocol because `ARCHITECTURE.md § 5.3`'s fallback rule is a statement about error types: only `ProviderTransientError` is `retryable`. `chat` is declared `def … -> AsyncIterator` so a real async generator satisfies it under `mypy --strict` (Decision Log; `ARCHITECTURE.md § 5.1` updated). These shapes are **internal** — no TS mirror, `server/schemas.py` is untouched. 43 new tests. Nothing imports them yet; `P1-02` is the first adapter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| P1-02 | `openai` adapter (streaming, tool calls, vision, configurable base URL)              | P1-01        | DONE   | 2026-09-17.`models/providers/openai.py`: `OpenAIProvider` over Chat Completions, plus the `ProviderConfig` (base URL, model table, `unknown_model`, `max_tokens_field`) that is the **only** thing `P1-05`/`P1-06` need to add — no subclass, no second client. Streaming, tool calls, vision (`ImagePart` → a `data:` URL), JSON mode, stop sequences and `stream_options.include_usage`, so `P1-09` has tokens to count; `Usage.cost_cents` is worked out from the model table (USD per Mtok, a dated snapshot). The key is an injected `KeyLookup` called once per request — `P1-07` swaps in the DPAPI vault — and a test asserts it is read per call and that `vars(provider)` holds no key. Errors: 401/403 → auth, 404 → capability, 429/5xx/timeout/connection → **transient** (the only kind `P1-08` may fall back on), anything else → protocol, and **never a response body**, tested with a key embedded in a fake error body. The capability gate refuses tools/JSON/vision the model lacks *before* the request. A tool call is emitted whole or not at all, and the argument buffer, the call count and one SSE event are all capped (`REVIEW.md § 2`). The adapter reads SSE itself rather than using `httpx2.EventSource`, so the reader is closed before the response on the preemption path (Decision Log). `httpx2` moved from a dev to a runtime dependency. 40 new tests, recorded-response style over `httpx2.MockTransport`, plus a live smoke test skipped without `AEGIS_OPENAI_TEST_KEY` (`REVIEW.md § 6`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| P1-03 | `anthropic` adapter                                                                  | P1-01        | DONE   | 2026-09-17.`models/providers/anthropic.py`: `AnthropicProvider` over the Messages API, pinned to `anthropic-version: 2023-06-01`. Streaming text, tool calls, vision, stop sequences, and usage assembled from both ends of the stream (`input_tokens` with `message_start`, cumulative `output_tokens` with `message_delta`), cached tokens counted as input rather than dropped. Four things are genuinely not configuration, which is why this is a module and not a `ProviderConfig` (Decision Log): the system prompt is a **top-level field**, a tool result is a block inside a **user** turn so adjacent same-role turns must be coalesced or the API refuses them, `max_tokens` is **required** (`DEFAULT_MAX_TOKENS` when the request names none), and the stream is named events addressed to a **block index**. **No JSON mode** — every model advertises `json_mode=False`, so the capability gate refuses before the wire; a temperature above 1 is refused the same way, since `ChatRequest` allows 0–2 for OpenAI's sake. `capabilities()` falls back to `ANTHROPIC_UNKNOWN` (vision + tools, **price `None`**) for a model newer than the table, so a current model is usable and a typo still fails as the API's own 404. Unlike `P1-02`, an image in a tool result survives — this API can carry one. The SSE reader, status map, cost sum and client factory moved to `models/providers/common.py` so the second adapter copies nothing (Decision Log); `P1-02`'s tests passed through the move unchanged. 61 new tests (360 pytest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| P1-04 | `google` adapter                                                                     | P1-01        | DONE   | 2026-09-17.`models/providers/google.py`: `GoogleProvider` over the Gemini API (`:streamGenerateContent?alt=sse`), the third and last wire format. Streaming text, tool calls, vision, JSON mode (`responseMimeType`), stop sequences, usage restated per chunk so the last one wins. Gemini disagrees with the other two in more places than they disagree with each other, and four are load-bearing (Decision Log): the assistant is called **`model`** and there are only two roles; tool results are addressed **by name**, so `_call_names` resolves a `tool_call_id` back to the call it answers and the ids in a Gemini `ToolCall` are **minted by this adapter** (a result for a call not in the conversation is refused, never sent under a guessed name); **tool calls arrive whole** (`args` is an object, not a fragmented string), so there is nothing to buffer; and there is **no tool-call finish reason**, so `resolve_finish_reason()` decides from what came back — tool calls + `STOP` → `tool_calls`, while `length`/`content_filter` still win because they say the turn was cut short. A `thought` part is dropped rather than rendered as the answer, and `thoughtsTokenCount` is added to output tokens because the user is billed for it. A prompt blocked before generation (`promptFeedback.blockReason`) finishes `content_filter` — a refusal is an outcome, not an error. The key goes in `x-goog-api-key`, **never `?key=`**, and the model id in the path is percent-escaped. `validate_key` treats a 400 as a rejected key, because this API answers a bad key with 400 and the only way to tell which 400 it is would be to read a body that may quote the key. 65 new tests (425 pytest)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| P1-05 | `nvidia`, `openrouter`, `custom` as base-URL configs over `openai`             | P1-02        | DONE   | 2026-09-18.`models/providers/compatible.py` — three `ProviderConfig`s and **no client**: `NVIDIA` (`integrate.api.nvidia.com/v1`), `OPENROUTER` (`openrouter.ai/api/v1`) and `custom_config(base_url)` for a user-entered gateway. All three answer `COMPATIBLE_UNKNOWN` for a model this build cannot enumerate — permissive on vision/tools/JSON so a model the user's key really serves is not refused, conservative on `ctx_window` (32 768) because a window is a budget and guessing high costs a 400 mid-task, and **price `None`**, never free (Decision Log). `normalise_base_url()` validates the one URL a user types, because it decides where their key is sent: `https` anywhere, `http` **only to loopback**, no credentials/query/fragment, length-capped, and the refusal never quotes the address back. `ProviderConfig` gained `key_check_path` so `openrouter`'s Test button asks the authenticated `/key` rather than its public `/models`, which would have called any string a working key. 42 new tests (467 pytest), incl. a streamed call over each config asserting the URL and bearer header. No TS moved — these shapes are internal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| P1-06 | `ollama` adapter + auto-detect on `localhost:11434`                                | P1-01        | DONE   | 2026-09-19,**inherited WIP** from 2026-09-18 (`RECOVERY.md § 6.2`). `models/providers/ollama.py`: `OllamaProvider` **composes** `OpenAIProvider` over a config pointed at `/v1`, so every byte on the wire is `P1-02`'s code and `chat()` returns the inner generator directly, keeping the stop path one link long. It is a module rather than a sixth `ProviderConfig` because two things about a local server are not a base URL (Decision Log): it takes **no key** (`ProviderConfig.requires_key`, new, is `False`; a saved key is still sent, for a proxy in front), and its model table is whatever the user pulled — so `refresh()` asks `/api/tags` and `/api/show` and `capabilities()` stays synchronous. A local model is priced **`0.0`**, not `None`: the inference is on the user's hardware, so free is a fact and `P1-09` must not pause a task that costs nothing. Unknown-model defaults are **conservative on vision** (Ollama answers a vision question for a text model as if the image were not there) but permissive on tools, and `ctx_window` is the **server's** 4096, never the model's trained window, because Ollama drops the overflow silently. `OLLAMA_HOST` is honoured through `P1-05`'s `normalise_base_url` — `http` loopback-only, since the payload is observations of the user's screen. **Two defects found and fixed while verifying the inherited work**, each with a mutation-checked regression test: discovery was **serial**, so a server that accepts and then stalls could hold startup for `MAX_MODELS` × the per-call timeout — it is now concurrent (8 at a time) under one `DISCOVERY_BUDGET_S`, measured 4.44 s → 1.11 s for 10 models; and `detect()` **raised** for a `base_url` argument it could not use, despite documenting that it never raises, which is the path `P1-10` will call with a user-typed URL. 45 new tests (509 pytest)                                                                                                                                                                                                                                                                                                                      |
| P1-07 | Key vault on DPAPI via`keyring`; never argv/logs/renderer; masked display            | P0-05        | DONE   | 2026-09-19.`storage/vault.py`: `KeyVault` stores one key per provider under the single Credential Manager target `Aegis`. It **constructs `WinVaultKeyring` itself and never calls `keyring.get_keyring()`** (Decision Log) — invariant 9 is about DPAPI, and `keyring`'s backend resolution can be pointed at a plaintext file by a `keyringrc.cfg` or any backend plugin on `sys.path`. `get_key()` reads through to the OS on every call and `lookup()` hands an adapter a `KeyLookup` callable rather than a string, so the `P1-02`..`P1-06` seam is now real and nothing that outlives a request holds a key. `set_key()` strips surrounding whitespace (a pasted key carries a newline), caps at 1024 chars, and **refuses any control character** — a key goes straight into an `Authorization` / `x-api-key` / `x-goog-api-key` header, so a CR or LF in one is header injection. `mask_key()` is the only display form: first 3 + last 4 (`sk-…abcd`), and nothing at all below 16 characters. Every log line names the provider and the action; every `VaultError` names the backend exception's **type**, never its message, because the value that backend was handed is the key. No route, no bridge member, no renderer path was added — `P1-08` is the first caller and `P1-10` the first screen. 38 new tests (547 pytest). `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-19`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| P1-08 | Router: role map (Planner/Grounder/Utility), fallback chain, capability gate           | P1-02..P1-06 | DONE   | 2026-09-20.`models/router.py` — the first thing to import every adapter **and** the vault. `RoleMap` is `{role: RoleRoute}` for all three of `ARCHITECTURE.md § 5.2`'s roles, each a `primary` plus up to three `fallbacks` of `ModelChoice` (`provider_id`, `model`, and § 5.2's *params* as `temperature`/`max_output_tokens`); it is bounded and refuses a chain that names the same model twice. `RoleRequest` is a `ChatRequest` with **no `model` field**, because each attempt in a chain runs a different one (Decision Log); `bind()` merges the saved params under whatever the request itself names. **Fallback** fires on `ProviderTransientError` and nothing else, and **only before the first delta reaches the consumer** — after that a restart would repeat timeline text and re-issue a tool call. **The gate** (`ensure_capable`) checks the **primary only** and refuses at task start naming the model and what is missing; an incapable fallback is dropped from the chain with a log rather than failing the call. `ProviderPool` owns one adapter per provider id (they own connection pools), holds a `KeySource` and never a key, and re-validates a saved `custom` address with `P1-05`'s `normalise_base_url` on every build. `ProviderUnavailableError` is a `RouterError`, not a `ProviderError`, so a missing gateway address is not retried around. The router closes the adapter's generator in a `finally`, because `async for` does not and preemption abandons mid-stream. 59 new tests (606 pytest), 4 mutations checked red. **Not** here: the budget guard (`P1-09`) and any route, bridge member or screen (`P1-10`) — nothing imports the router yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| P1-09 | Budget guard: per-task + per-day ceilings, live cost events                            | P1-08        | DONE   | 2026-09-20.`models/budget.py` (`BudgetLimits` / `BudgetStatus` / `BudgetGuard`) + `storage/usage.py` (`UsageLedger`, the core's first long-lived DB connection) over a new **ninth table** `usage` (`m0002_usage.py`; `ARCHITECTURE.md § 7` updated) — a per-day ceiling that an app restart clears is not a ceiling. `ModelRouter.chat` gained an optional `guard` and a `task_id`: it `check()`s before anything reaches the wire and records every `UsageDelta` against the model that actually answered, including a fallback. **A breach is refused at the door, never mid-flight** — usage arrives at the end of a stream, so the money is already spent and the refusal belongs at the start of the next call (Decision Log). Ceilings are per-task and per-day, in **both cents and tokens**: an unknown price is never summed as zero, so on a `custom` gateway or an unlisted OpenRouter model a cents-only guard would never fire — the token backstop (10M/100M, far above any honest task) is what does. Defaults $1.00/task, $10.00/day. `Spend.unpriced_calls` is carried into every message and event, so a short total says *at least*. `cost.updated` is published per call with the call, both totals, the limits and `breach` — numbers and model ids only (`ARCHITECTURE.md § 9.2`). 46 new tests (652 pytest), 4 mutations checked red. **Not** here: persisting user-chosen limits and the spend meter, which are `P1-10`'s screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| P1-10 | Models screen (`UI.md § 8.4`): role cards, provider cards, Test button, spend meter | P1-08, P0-09 | DONE   | 2026-09-20. The first screen with a model behind it, and the first route that writes anything.**Core:** `storage/settings.py` (the `settings` table, bounded values, an unreadable document reads as absent) and `models/service.py` — one owner for the settings document, the vault and the ledger, and the only place the wire shapes meet `RoleMap`/`BudgetLimits` (Decision Log). New routes on `§ 9.1`: `GET /models/catalog`, `POST /models/validate`, `GET /models/spend`, `GET`/`PUT /settings`, and `PUT`/`DELETE /models/keys/{provider_id}` — **the only route that takes a key, and nothing reads one back**. The catalogue makes **no call to a vendor**: it is this build's tables plus whatever Ollama on loopback answers, so the screen opens on a machine with no internet, and a provider that cannot be enumerated reports `free_text_model`. A malformed request is now a **422 that echoes none of it**, app-wide. `create_app` takes an optional `ModelService`; without one every model route answers `503`. **Renderer:** `ModelsScreen` + the `UI.md § 12` components `ModelRoleCard`, `ProviderCard` and `SpendMeter`, over `stores/models.ts` (a saved/draft split) and `lib/models-client.ts`. The spend bar reads the last `cost.updated` off the stream, so it moves during a task without this screen polling. **Exercised in the built app over CDP**: mapped all three roles, set a $3.50/day ceiling, saved, saved a key into the real Credential Manager (shown back as `sk-…WXYZ`, gone from the DOM), tested Ollama in 11 ms, restarted the app and found it all still there — then removed the key and the settings row again. That run found and fixed one real defect: `edit(value)` lost one of two draft changes made in a single React batch, so `edit` takes an updater (Decision Log), with a regression test. 141 new tests (720 pytest, 274 renderer). Not on `REVIEW.md § 5`'s gated list and **nothing was added to the preload surface** — the screen rides the existing `core.request` — but it is the first code path a key travels along, so § 5's Secrets section was walked anyway: `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-20` |

**Gate:** user pastes a key for any supported provider, hits Test, sees a real completion; a killed primary falls back to the secondary; keys survive an app restart and never appear in any log.

---

## 4. P2 — Perception

| ID    | Task                                                                                                              | Blocked by   | Status | Notes                                                |
| ----- | ----------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ---------------------------------------------------- |
| P2-01 | Per-monitor DPI awareness + a virtual-desktop coordinate model                                                    | P0-05        | DONE   | 2026-09-21. `perception/display.py` (the model) + `perception/win32.py` (ctypes only, own `WinDLL`). **One space: physical pixels on the virtual desktop**, no DIP anywhere in the core (Decision Log). `ensure_dpi_awareness()` sets Per-Monitor V2 as the **first** thing `main()` does and never raises; `query_layout()` **refuses** a thread that is not per-monitor aware, checked per thread. `DisplayLayout` is a validated snapshot (one primary at the origin, DPI 96–480, work area inside bounds, `SM_*VIRTUALSCREEN` = the monitors' bounding box, ≤ 16 monitors; a torn read is retried 3×), with gap-aware `monitor_at()`, `to_absolute()` for `SendInput` `ABSOLUTE\|VIRTUALDESK` (midpoint rule, dead zones refused), a DPI-inclusive `fingerprint`, and `verify_layout()` → `LayoutChangedError` for `RECOVERY.md § 3.3`. 54 µs per query. `MOUSEEVENTF_VIRTUALDESK` added to `actuation/win32.py`. 74 new tests (794 pytest): mixed-DPI layouts with negative coordinates and dead zones, an exhaustive per-pixel mapping check up to 32 768 px, live reads, the unaware-thread refusal, and a **real `SendInput` → `GetCursorPos` round-trip** over ~230 points that lands on every pixel exactly. 2 mutations checked red (the naive `65535/(w−1)` mapping fails the live round-trip too). Built app: core logs `display.dpi_aware PER_MONITOR`. **Only verified on one monitor at 200 %** — multi-monitor, 125 %/150 % and hot-plug are covered by synthetic layouts, not hardware. Unblocks `P3-01`'s absolute `move_to` |
| P2-02 | `screen.capture()` via `mss`: monitor / window / region, WebP encode                                          | P2-01        | DONE   | 2026-09-21. `perception/screen.py`: `capture(MonitorTarget \| WindowTarget \| RegionTarget) -> Frame` (full-res BGRX + region + the `DisplayLayout` it was taken under), `Frame.encode() -> Screenshot` (≤1280 px long edge — a larger `max_edge` is **refused** — box filter, WebP q80 method 0). **Measured on a 3200×2000 panel: capture + encode median 115 ms, p90 128, worst of 30 143 ms** (budget 150); a test asserts the median. DPI check (`query_layout()`) runs **before** `mss` is ever constructed, because `mss` sets process DPI awareness on construction; the layout is **re-verified after the grab** (`LayoutChangedError`). One lazily-built `mss` object per process, dropped on failure. Windows are captured as seen on screen; minimised / hidden / cloaked / off-screen each refused with its own message. Region pixel cap = one 8K monitor. New runtime deps `mss>=10.2`, `pillow>=11.0`. 62 tests (856 pytest), incl. live captures of a real Win32 window; 6 mutations all red. **Nothing redacts yet — P2-05 must run before any `Screenshot` leaves the process** |
| P2-03 | `uia_tree.walk()`: foreground window → element list with role/name/value/bbox                                  | P2-01        | DONE   | 2026-09-22, **inherited WIP** from 2026-09-21 (`RECOVERY.md § 6.2`: the module was on disk with no tests and no declared dependency, then committed as `1c45d91 "next task completed"`). `perception/uia_tree.py`: `walk(hwnd=None) -> UiaTree`, the foreground window by default, flattened to `UiaElement`s in document order (`id`, `parent`, `depth`, `role`, `name`, `value`, `bbox`, `enabled`, `focused`, `offscreen`, `is_password`, `automation_id`, `class_name`, `runtime_id`). **One cross-process call:** a `CacheRequest` over the control-view subtree fetches every property at once. Bounded: UIA connection + transaction timeouts 2 s (Windows' transaction default is 20 s), ≤ 5 000 elements, ≤ 64 levels, text cut to 1 000 chars, `truncated` says when a cap bit. DPI check first, layout re-verified after, like capture. A password field's value is dropped at the first moment the core holds it. **Defect found and fixed:** a cached property an element does not support reads as its *default*, so every button came back with `value == ""` (752 of 756 elements in VS Code) — `IsValuePatternAvailable` is now cached and a value kept only when it is `True`. Also: `comtypes>=1.4` declared (it was installed by hand, so a fresh venv could not import the module), a mypy override for it (no stubs; `mypy --strict` was failing), and a lock around the one-time type-library generation. 66 tests (922 pytest), incl. a live window with a button, a disabled button, a text box and an `ES_PASSWORD` box: roles/names/values/state exact, bboxes equal `GetWindowRect` to the pixel, the typed password appears **nowhere** in the tree, a hung window fails in 2.0 s, a fresh thread joins COM and walks twice. 9 mutations, all red (the two timeouts only after adding a fake-COM test — the live hang is caught at *connection*, where Windows' default is also 2 s). Measured: 5-element form ~20 ms warm, VS Code 878 elements; first walk in a process pays ~200 ms of COM start-up. **Known gap:** an Electron app that has not enabled accessibility (Notion, measured) exposes its whole page as one empty `Document` — 15 elements, all chrome. Not a read failure; it is `P2-07`'s case |
| P2-04 | Tree pruning + relevance ranking, cap 200 elements                                                                | P2-03        | DONE   | 2026-09-22. New pure `perception/prune.py`: `prune(tree, goal="", limit=200) -> PrunedTree` of `Candidate`s (the `UiaElement` with its **walk `id`**, the **visible** bbox, nearest kept ancestor, score), in **document order**, plus `omitted` and `truncated`. Prunes offscreen / arealess / outside the window or every monitor (dead zones), unnamed non-interactive elements, labels repeating the element they sit in, row cells repeating the row, and structural wrappers named like the button inside. Ranks interactive > text > structure, named, enabled, goal words in a ≤ 12-word label; cells, scrollbar parts and slivers demoted. Window + one focused element always kept (Notepad reports **three** focused panes). Live: Explorer 243 → 181, editor 808 → 200 (14 omitted) in **3 ms**, Notepad 54 → 35; "rename the file 1028" puts that row first and *Rename* second. 44 tests incl. a live `FormWindow` walk; 15 mutations all red. **Nothing calls it yet**: `P4-04` passes the goal. **Not verified** against `P4-12`, which does not exist yet: the weights are reasoned and checked on three windows, not measured on tasks |
| P2-05 | **`redact.py`**: password fields + secrets-regex → black boxes in image bytes, values stripped from tree | P2-02, P2-03 | DONE   | 2026-09-22. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-22.** New `perception/redact.py`: `redact(frame, trees) -> Redacted` (painted copy of the frame, cleaned trees, boxes); its `encode()` is the only public way to a `Screenshot`. Password fields, secret-labelled fields (own name or preceding `Text`), their descendants, and credential-shaped text (API keys, PEM, JWT, Luhn cards) → value stripped, `redacted=True`, 4 px-padded box black in the full-res bytes. **Every unvouched pixel is black**: outside walked windows, under unwalked windows above them, all of a truncated walk. Refuses a tree from another layout or a window that moved since its walk. `redact_text()` for `P5-12`. Enforced by an AST scan of the core (`_encode`/`_to_image`/`.pixels`/`Screenshot(`/`Redacted(`/`redacted=`) and by `prune()` refusing an unredacted tree. Golden test on a real password box off the real screen, in bytes and WebP; live occlusion test. 20 ms for a 3200×2000 monitor. 17 mutations red, plus a planted bypass. **Not covered:** text with no tree behind it (canvas, terminal buffer — `P2-07`), and a field scrolling inside an unmoved window between walk and capture (padding only) |
| P2-06 | Set-of-mark renderer: numbered overlays on candidate elements                                                     | P2-04, P2-02 | DONE   | 2026-09-23. New `perception/mark.py`: `mark(redacted, pruned) -> Marked` — a coloured outline and a numbered chip over every candidate, and the encoded `Screenshot` with the ids it drew. It takes a **`Redacted`**, never a `Frame`, so a mark can only land on pixels redaction already cleared to leave; the chip carries the candidate's **walk `id`**, which is what `P2-08` will resolve, never a fresh 1..N numbering; the marks go on **after** the downscale, at the size the model sees, which is why `Frame._encode` split into `_to_image` + a new private `screen._encode_image`, both added to `test_redact.py`'s source-scan allow-list (with a parametrised case proving the scan catches both spellings of the new call) rather than routed around. **Only digits are ever drawn** — asserted by recording every string that reaches Pillow — so nothing redaction stripped can be painted back; a real redacted password box is still black after marking; and the log carries counts only. One walk at a time (ids are unique within a walk, not across walks). A candidate off the region or under `MIN_BOX_PX` at output scale comes back in `Marked.unmarked` instead of being offered as an id with no box. Palette of 6 cycled in document order; chips avoid each other and stay inside the image. 26 new tests (1072 pytest), incl. a live walk→capture→redact→prune→mark of a real window, and the result was **looked at**: five boxes, legible numbers, the password field black under its mark. Measured: ~60 ms to draw 200 marks on 1280×800; the budget test asserts the **drawing**, since the encode is `P2-02`'s cost and its own budget test is content-dependent (`P2-10`). Not on `REVIEW.md § 5`'s gated list, but it widens the encode allow-list, so § 5 was walked: `SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-23` |
| P2-07 | OCR fallback (Windows OCR via winrt, Tesseract fallback)                                                          | P2-02        | DONE   | 2026-09-24. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-24** (it changes redaction). New `perception/ocr.py`: `blind_regions(tree)`, `read_region(engine, pixels, region)`, `WindowsOcr` (`Windows.Media.Ocr` via the `winrt` 3.2 projection, one daemon thread, 3 s timeout + 1 s grace, `OcrUnavailableError` naming the Settings page when no OCR language is installed). **`redact()` is the only caller**: after the tree-based painting it reads each blind region off the *painted* frame — so OCR never sees a secret field or another window, and `ocr.py` never touches a `Frame` (the source scan needed no new allowance) — and puts every line through the credential scanner and `SECRET_LABEL`: a hit is blacked out (padded) in the pixels and replaced in the text (`Token [redacted]`; a secret-label line loses all its text). **A blind region is read and redacted, or painted black** (`Redacted.unread`) — on no engine, no language, a failure, a timeout, or `ocr=None`. `Redacted.text` carries the lines. Every line is scanned **whole, before** the `MAX_LINES` cap and the 1 000-char cut. Blind-region rule **measured, three times over** (Decision Log): large (≥ 20 %), no value, and < 25 % covered by describing elements from **anywhere** in the tree. **Live:** Windows Terminal showing a fake key → key line black in the real pixels, `[redacted]` in text, other lines read, 71–115 ms; the IDE's code editor and integrated terminal are blind (Monaco and xterm draw with no tree), and when another window covers them redaction has already blacked them so OCR reads nothing; Notepad, the taskbar and a 592-element Chromium window are not blind. `blind_regions` 7.5 ms on 1 927 elements; redaction on the IDE 145 ms with OCR vs 81 without. 45 new tests: every blind-region clause on shapes measured live, coordinate mapping, the real engine on Pillow-drawn text, timeout, crash-and-carry-on, no-language, redaction with a scripted engine (credential, secret label, clean line, failure → black, opt-out → black, OCR sees the painted frame, scan-before-cap, key past the text cut, no text in logs), and a **golden** test: a `CanvasWindow` that paints a key with GDI and no tree behind it is black in the frame bytes and the WebP. New runtime deps: seven `winrt-*` packages. **Split out:** Tesseract → `P2-12`; OCR lines as click targets → `P2-13`. **Not verified:** OCR quality at 100 %/125 % scaling (only 200 % here), non-Latin scripts, and a key OCR misreads with a space inside it (no longer matches — the stated residual risk) |
| P2-08 | `grounding.resolve(element_id) -> point`, re-verified against a fresh capture                                   | P2-04        | DONE   | 2026-09-24. New `perception/grounding.py`: `resolve(pruned, element_id, *, offered=None) -> Grounding` (`point`, fresh visible `box`, `hwnd`, `layout`, `moved`, `hit` ∈ target/inside/container). The id is model output: a bool, a non-int, an id that is not a candidate, or one outside `offered` (`Marked.ids`) is refused **before anything is read**. Then a **fresh observation**: window checks (closed / minimised / other virtual desktop, each its own message), a fresh `walk()` of the window put through `redact_tree()`, the same `DisplayLayout` or `LayoutChangedError`, and the element re-found by **`runtime_id`** with the same role and the same redacted name (*Play* → *Pause* refuses), enabled, not offscreen, with a visible part. The point is the centre of the **fresh** visible box — physical pixels already, no DPI step. **Still on top** is two hit tests: `WindowFromPoint`→`GA_ROOT` must be the window (new `win32.root_window_at`), then new `uia_tree.hit_chain()` — `ElementFromPoint` climbing the control view, stopping at the target or any ancestor, bounded at `MAX_DEPTH`, same 2 s timeouts (`_automation()` factored out of `_read`) — must meet the target, something inside it, or a bare ancestor (coarse providers). Any other element refuses. Every refusal is a `GroundingError` ending *Take a fresh look at the screen before choosing again.*; no name ever reaches a message or the log. **Measured live:** runtime ids 100 % stable across walks (1 769 elements); a Chromium app 37/40 candidates ground; **Explorer 69/83 ground, 8 correctly refused as disabled, 6 refused as covered** — 3 true (incl. the address box, whose centre is a breadcrumb *button*), 3 false: title-bar min/max/close, which UIA answers under another id (Decision Log; the `window` tools own them). Explorer 139 ms median per resolve, dominated by the walk. 46 new tests in `test_grounding.py` + 12 in `test_uia_tree.py` (1130 pytest): every refusal on hand-built trees, and live on a real window — a button grounded onto its own pixels (and where `WindowFromPoint` delivers), a password box grounded with nothing read, disabled refused, the window **moved** after observation (point follows), covered by another window, covered **inside its own window** by an `SS_NOTIFY` label, *not* covered by a click-through label, closed, and a hung window's hit test failing within the timeout. Found and fixed by the tests: the climb made one wasted cross-process call past its bound. `FormWindow` gained `move_to()` and `cover()`, run on its own thread. **Nothing calls it yet** (`P4`); the click that uses a `Grounding` must `verify_layout()` its `layout` first (`P3-01`). **Not verified:** multi-monitor and 125/150 % on hardware; how often real providers' runtime ids change mid-task (e.g. virtualised list rows) — that shows up as a refusal, not a misclick |
| P2-09 | Perceptual hash on observations (feeds the stuck detector)                                                        | P2-02        | DONE   | 2026-09-24. `perception/phash.py`: 256-bit dHash of a **`Redacted`** (17x16 grey grid, 1-level dead band), `PerceptualHash.distance()` / `near_identical()` (≤ `NEAR_IDENTICAL_BITS` = 5), `.hex` for `observations.phash`. Real screen: static 0 bits, dropdown 9, dialog 33, ~11 ms. No caller yet — `P4-06` is the first |
| P2-10 | Decide `test_capture_and_encode_fit_the_budget`: the P2-02 150 ms budget is missed on this machine                 | P2-02        | DONE   | 2026-09-24. **Decided: fixed content for the gate, the live screen as an opt-in benchmark.** Measured: on the live screen grab ~57 ms + downscale/encode ~55 ms idle; with every logical CPU busy, 77 + 127 ms, and even the thread's own CPU time doubled (hyperthreads, turbo), so no timing on a shared machine is load-proof. That, not content, explains 186/362 ms. The gate is now `test_downscaling_and_encoding_a_busy_desktop_fit_the_budget`: a seeded 3200x2000 frame, harder than a real desktop (236 KiB vs ~97), median < 150 ms (68 idle). It catches WebP method 4 and a skipped downscale; it does **not** catch Lanczos (+tens of ms). `test_capture_and_encode_fit_the_budget` is unchanged but runs only with `AEGIS_PERF=1` (passed here when run that way), and `P8-04` runs it |
| P2-11 | Fix `test_golden_a_real_password_box_is_pixel_black_off_the_real_screen`: its **precondition** samples a black glyph | P2-05        | DONE   | 2026-09-24, by **GitHub PR #1** (Copilot agent, merged as `9f75b90`), after this precondition turned `main`'s CI red on the P2-05 and P2-08 pushes. The fix is the one this row asked for: the precondition now asserts the password box is not **entirely** black before redaction, instead of sampling one pixel that lands on a password dot at 192 DPI. It still guards what it is for — redaction must be what makes the box black. Verified: passes 3/3 locally where it failed every run before, and CI on `72ab485` (P2-07 merged on top) is green. PR #2 (`copilot/fix-failing-github-actions-job`) carries the same change and nothing else, so it is redundant and should be closed, not merged |
| P2-12 | Tesseract OCR fallback for machines with no Windows OCR language | P2-07 | BLOCKED | Split from P2-07 on 2026-09-24 (Decision Log). Today such a machine fails **closed**: blind regions black, a message naming Settings > Language. Needs a packaging decision first — bundled binary + language data (size, antivirus) versus a user-installed one at a fixed path, **never** one found on `PATH` — and a machine with Tesseract to verify on. Same `OcrEngine` protocol, so `redact()` does not change. **2026-09-24: BLOCKED on the owner's packaging decision** (asked; deferred to P7 packaging). Still no Tesseract on the dev machine. Unblock by choosing bundled vs user-installed-at-a-fixed-path |
| P2-13 | OCR lines as click targets: offer `Redacted.text` lines to the model and ground them by re-reading the region at click time | P2-07, P2-08 | DONE   | 2026-09-24. `grounding.text_targets(redacted)` offers `Redacted.text` lines by index (never a redacted one, only one owned by exactly one walked window's blind region); `grounding.resolve_text(redacted, line_id, offered=…)` re-observes the window **through `redact()`** and requires the same text within 2 px on every edge, plus `WindowFromPoint`. Live on a self-drawing window: every line grounds inside its drawn band; redrawn text and a scrolled line refuse. Nothing calls it yet — `P4` must present the ids to the model and `P3-01`/`P5-02` click a `TextGrounding` the way they click a `Grounding` |

| P2-14 | `test_downscaling_and_encoding_a_busy_desktop_fit_the_budget` fails under background load | P2-10 | DONE | 2026-09-25. **Cause: machine state, not code** — on battery (Balanced plan) with other programs busy it measured ~305–332 ms, including on the untouched `6aa7dfe`, and ~92 ms minutes later. **Decided:** a load-robust ratio gate plus structural pins. `test_downscaling_and_encoding_a_busy_desktop_keep_pace_with_a_fixed_workload`: the busy-desktop encode timed alternately with a fixed 1 MB zlib workload, median per-pair ratio < 3.2 (measured 1.25–1.62 at 0–12 busy processes; WebP method 4 3.69–5.37). New `test_the_encoder_runs_libwebps_fastest_method` pins `method=0` with no timing. The absolute 150 ms stays the `AEGIS_PERF` live benchmark. Mutations: method 4 and a skipped downscale both red |

**Gate:** on a live Explorer window the core returns a correct element list, a marked screenshot, and grounding for a named button — with a password field on screen fully black-boxed.

---

## 5. P3 — Actuation + safety spine

> **This is the phase that decides whether the product is trustworthy. Do not compress it.**

| ID    | Task                                                                                                   | Blocked by          | Status | Notes                                                                                                                                                                                                                                |
| ----- | ------------------------------------------------------------------------------------------------------ | ------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P3-01 | `SendInput` wrapper: move, click, double, right, drag, scroll, type (unicode), key combos            | P2-01               | DONE | 2026-09-25, at the owner's request. Finished on top of what P3-04 landed: `move_to` / `click_at` (double, right) / `drag_to` in physical virtual-desktop pixels via P2-01's `to_absolute()` with `MOUSEEVENTF_VIRTUALDESK`, each re-checking the display layout and a drag validating its whole path before pressing; `press_keys("ctrl+shift+n")`; the full named-key table in new `actuation/keys.py` (F1–F24, navigation, numpad, media; characters on the user's layout via `VkKeyScanW`); keys now carry `MapVirtualKeyW`'s scan code and the extended flag from `E0` or Microsoft's list (Left used to arrive as numpad 4). Live: `move_to` through the real controller lands on every probed pixel of this desk. 9 mutations all red |
| P3-02 | Signature tagging: every synthetic event carries`dwExtraInfo = AEGIS_SIGNATURE`                      | P3-01               | DONE   | 2026-09-09.`actuation/signature.py` + `SendInputBackend`, which is the only caller of `SendInput`. Landed early because P3-04 is meaningless without it                                                                        |
| P3-03 | Human-like motion (bezier path, small jitter, sane dwell)                                              | P3-01               | DONE | 2026-09-25. New `actuation/motion.py` (`Motion`, `HUMAN`, `plan_path()`: cubic Bézier bowing ≤ 15 %, smoothstep easing, ±1 px jitter never on the last point, Fitts'-law 0.08–0.45 s at ~120 Hz, 40–120 ms dwell). `InputController(motion=HUMAN)` travels from `GetCursorPos` for `move_to` / `click_at` / `drag_to`, falls back to a straight line off-desk, and waits on the preemption signal between steps. Opt-in; P4 passes `HUMAN`. Live: lands exactly on the target in time. 11 mutations all red |
| P3-04 | **Low-level hooks** `WH_MOUSE_LL` / `WH_KEYBOARD_LL`, ignoring signed events                 | P3-02               | DONE   | 2026-09-09.`actuation/preempt.py`: dedicated thread, hooks + message loop, no I/O in the callback. Measured 1.06 ms median from injection to signal set (budget 5 ms)                                                              |
| P3-05 | **Preemption**: real input → abort in-flight action, release all held modifiers, PAUSED_BY_USER | P3-04               | DONE   | 2026-09-09. Abort + full modifier release measured at**6.3 ms median / 9.3 ms worst** over 10 runs (budget 100 ms). `PAUSED_BY_USER` itself is the listener seam — the task state machine arrives with P4                   |
| P3-06 | **Kill switch**: global hotkey in MAIN → hard stop, release keys, kill job children, freeze     | P0-02, P3-05        | DONE | 2026-09-24. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-24.** MAIN: `main/hotkeys.ts` (binding: ≥ 2 modifiers + 1 key, default `Control+Alt+Shift+Q`, saved in `%LOCALAPPDATA%\Aegis\hotkeys.json`; a rebind registers the new shortcut before releasing the old; unarmed → `hotkeys.get()` refuses), `main/kill-switch.ts` (`POST /v1/kill` with a **100 ms** deadline, else terminate), `supervisor.terminate()` (`TerminateProcess` on the handshake PID **and** MAIN's child — they differ in dev — then a fresh core with a clean budget). Tray *Stop the agent* is the same trigger and shows the live binding. Core: `actuation/killswitch.py` (sticky freeze + release sweep of every attached `InputController`, which now checks it before, between **and after** events) and `POST /v1/kill` → `KillResponse`. **Measured on the real core:** acknowledged 2.8–7.8 ms; a core suspended with `NtSuspendProcess` dead **102 ms** after the press, launcher too, fresh core back, nothing left after stop. A live vitest against a real wedged two-process fake core asserts < 200 ms (~102). Built app: an injected Ctrl+Alt+Shift+Q reached the core (`kill_switch.engaged` in `core.log`), and closing the app took the core with it. **Left out on purpose:** a terminated core's held keys (`P3-15`); job children die through the Job Object `P5-08` must use (`taskkill /T` measured ~300 ms); the “Stopped by you” UI, the last five actions and showing an unarmed switch (`P3-14`); the audit-log entry (`P6-04`) |
| P3-07 | Watchdog: MAIN pings core 1/s; 2 misses while running → kill core                                     | P0-07               | REVIEW | 2026-09-25. **Only outstanding: launching the built app once by hand** (REVIEW §8.3) — a running Aegis instance held the single-instance lock all session, so the second launch exited, as designed. Everything else in REVIEW §1/§2 is walked. `main/watchdog.ts`: while a task is `RUNNING`, `GET /v1/health` every 1 s with a 1 s deadline; 2 misses in a row against the **same** core → `supervisor.terminate('watchdog')` (same `TerminateProcess` path as the kill switch, but **counted** against the 3-in-60 s budget). A round whose deadline fired > 250 ms late (sleep, blocked MAIN) is inconclusive. `main/task-activity.ts` learns `RUNNING` from forwarded `task.status` events; `TaskState` added to `server/schemas.py`, held equal to `tasks.status`'s CHECK. Live: a core wedged mid-task is dead **~2.5 s** later (worst case 3 s). **Armed but idle until P4 publishes `task.status`** |
| P3-08 | Guardian core:`evaluate()`, tier model, `rules.yaml` loader                                        | P0-05               | DONE | 2026-09-25. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-25** (it is the Guardian). `guardian/risk.py` (the `§ 10` table cell by cell, `escalate()` capped at DANGEROUS), `guardian/rules.py` + `rules.yaml` (closed schema, `safe_load`, **digest-pinned** by `RULES_SHA256`; `canonical_path()` through junctions, 8.3 names, streams, trailing dots, `\\?\`; UNC never opened), `guardian/policy.py` (`Guardian.evaluate(tool, params, ctx) -> Verdict`: FORBIDDEN / uncheckable → scope → signals + tier). `Autonomy`, `Decision` added to `server/schemas.py`. `rules.yaml` ships with an **empty** FORBIDDEN list — P3-09's; `§ 8.1`'s user-rule step deferred to P3-11 (Decision Log). 213 tests; 23 mutations red. Nothing calls it yet |
| P3-09 | FORBIDDEN list compiled in, not editable from UI, with unit tests per entry                            | P3-08               | DONE | 2026-09-25. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-25.** `rules.yaml`: 28 entries, 100 patterns (Windows/Program Files/Startup writes; credential stores, DPAPI keys, browser password/cookie databases and `Local State`, `.ssh`, `.gnupg`, cloud and token files, password managers, wallets; all of `%AEGIS_DATA%`, writes to `%AEGIS_INSTALL%`; autostart/security/service registry keys, `SAM`/`SECURITY`; disk wiping, boot config, backup deletion, Defender/firewall/UAC tampering, service and driver installs, log clearing, credential theft, autostart commands, encoded PowerShell). `%AEGIS_DATA%`/`%AEGIS_INSTALL%` computed by the core, never from env; command rules also match a de-obfuscated form. `tests/guardian/test_forbidden.py`: a denied example per entry **and per pattern** (a pattern nothing reaches fails), plus ordinary work that must stay allowed; the rules are unreachable from any route. 9 list mutations all red |
| P3-10 | Scope model: named scopes, path normalisation (realpath, symlinks,`..`), enforcement                 | P3-08               | DONE | 2026-09-25. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-25.** `guardian/scope.py` (`Scope`, `make_scope`, `load_scope`), `storage/scopes.py` + m0003 `scopes` table, `Guardian.check_target()` (the at-operation re-check), `guardian/win32.py`. **Four normaliser bypasses found by probing and closed** (Decision Log): device namespace (`\\.\C:\Windows` matched no rule), admin shares and loopback UNC, `NUL`, and symlinks to a share (realpath hung 42 s). Routes, the user-picked-folder trust path and the UI split to **P3-17**. 92 new tests incl. real junctions, symlinks, `subst`; 19 mutations all red |
| P3-11 | Approvals: request/resolve API, timeout auto-**deny**, `allow_always` with scoping             | P3-08, P0-08        | DONE | 2026-09-25. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-25** (it changes the Guardian). `guardian/approvals.py` (`ApprovalBroker`: `request()` / `resolve()` / `deny_all()`; deadline in the core, 30 s DANGEROUS / 60 s otherwise, **timeout denies**; kill switch, cancellation and shutdown deny all pending; one answer each), `guardian/allow_rules.py` + m0004 `allow_rules` (exact / tool_in_folder / tool_for_task, made from the pending call — the request names only the kind), `evaluate()` step 4 (trusted only, SAFE/CAUTION only, never over an echo or a deny), routes `POST /approvals/{id}`, `GET`/`DELETE /rules`. A NULL-passing `CHECK` in the new table was caught by a test and fixed. 44 new tests; 13 mutations all red |
| P3-12 | `ApprovalDialog` component to `UI.md § 5` spec incl. 200 ms input guard and Deny-default focus    | P3-11, P0-09        | DONE | 2026-09-25. `components/ApprovalDialog.tsx` + `lib/approvals.ts` (events → pending approvals, validated) + `stores/approval.ts` (guard / sending / notice), mounted in `AppShell`. Deny focused and never disabled; the 200 ms guard on Allow re-arms per question; Esc denies; focus trap; countdown from the event's timestamp; *Allow always* only with the core's `rule_kinds`; banner red unless the event's new `reversible` is true (core side added). Stories for every state; 30 behaviour tests; 12 mutations all red. Its own always-on-top window is **P3-19** |
| P3-13 | `OverlayHUD` window: click-through, cursor-avoidance, pause/stop, amber approval state               | P0-02, P0-09        | DONE | 2026-09-25. **SECURITY GATE: reviewed against REVIEW.md §5 — claude-code, 2026-09-25** (a new preload). `main/overlay.ts` (frameless, transparent, always-on-top, never focusable, `setContentProtection` → excluded from capture), `overlay-policy.ts` (click-through + dodge the cursor only while a task runs), `preload/hud.cts` + `hud-channels.ts` (`aegisHud`: subscribe/stop/showMain/deny — no allow), renderer `hud.html` + `OverlayHUD` + `hud-state`. **Hand run on the built code:** renders as designed, dodged top→bottom with a task running, stayed put idle, and the core's own capture of its rectangle showed the window behind it. Live test in `test_screen.py` that an excluded window is not captured (and is, once cleared). 10 mutations all red |
| P3-15 | MAIN's own modifier-release path, run**first** when the core dies mid-task                       | P3-06, P0-07        | DONE | 2026-09-26. `main/win-input.ts` (`koffi` → `GetAsyncKeyState` + tagged `SendInput` `KEYUP`s) and `main/modifier-release.ts` (all 8 modifiers that are down; never stops part-way). Wired **after** `supervisor.terminate()`'s kill and again on that exit (kill switch + watchdog), **first** in `onCoreExit` when a task was running (not when idle), and after a kill ack with `release_failures > 0`. Live: a held Shift and right Ctrl come up in < 20 ms. Parity test holds `AEGIS_SIGNATURE` to the core's. 11 mutations all red. Measured and rejected: a spawned helper (Python ~95 ms, PowerShell ~257 ms). An incident during the mutation run is in REMEMBER § 9 |
| P3-16 | Investigate `test_untagged_input_preempts` + `test_the_callback_sets_the_signal_within_5ms` failing in a full run | P3-04 | TODO | Found 2026-09-24: both failed in the session-start `pnpm test` ("untagged input did not preempt" within 250 ms), passed 14/14 run alone straight after, and passed in the end-of-session full run. This is the preemption test, so an unexplained miss is not a flake to ignore: decide whether the hook thread is starved or its hook silently dropped under full-suite load (`LowLevelHooksTimeout`), or the injected key is being eaten, and make the test say which |
| P3-14 | Preemption + kill-switch UX (`UI.md § 7`): edge flash, "You took over", Resume-with-a-note          | P3-05, P3-06, P3-13 | TODO   | From P3-06: MAIN logs each press (`kill switch: <outcome> in N ms`), but nothing reaches the renderer yet. This task needs an event or a push for “Stopped by you”, and a visible state when the switch is **unarmed** (another program owns the shortcut, so `hotkeys.get()` refuses) **From P3-13:** the HUD shows only when `window.setOverlay(true)` is called — decide when (a task starts / the main window hides) here. Its `paused` state already says "You took over — Aegis paused."; Resume and Resume-with-a-note go in `OverlayHUD`, which will need its preload to grow (a §5 review) |
| P3-17 | Scope routes (`GET/POST /scopes`), the user-picked-folder trust path, and the `ScopePicker` / Manage-scopes UI | P3-10, P0-09 | TODO | Split from P3-10 (Decision Log). Security-gated: a preload change. The renderer's core passthrough is **generic**, so a `POST /scopes` that takes any folder lets a compromised renderer widen the agent's reach. A folder may enter a scope only because the user picked it in the OS dialog: e.g. a bridge member that opens the dialog in MAIN and posts the folder to a core route MAIN's passthrough refuses to forward. Reconcile with `main/path-grants.ts` (the 2026-09-09 row says the scope model replaces it, not duplicates it). Build on `guardian.scope.make_scope` / `load_scope` and `storage.scopes.ScopeStore`; every refusal is `ScopeError`'s sentence (a 400) |
| P3-18 | Rules screen (`UI.md § 8.3`): list always-allow rules and revoke them | P3-11, P0-09 | TODO | Found during P3-11: `UI.md § 8.3` describes it but no task owned it, and it is what makes an *Allow always* revocable by a person (`REVIEW.md § 5`). Built on `GET /v1/rules` and `DELETE /v1/rules/{id}` (`AllowRuleInfo`: kind, tool, folder or task, created_at) |
| P3-19 | The approval dialog in its own always-on-top, focus-stealing window (`UI.md § 3`) | P3-12, P0-02 | TODO | Split from P3-12: the component exists and shows over the main window, but the main window may be hidden while the agent works (the HUD is all that shows), so a question must be able to surface on its own. MAIN opens a 480px modal window on `approval.requested` and closes it on `approval.resolved`; the only place the app steals focus. Security-gated if it touches the preload surface |

**Gate:** with a task running, (a) moving the physical mouse pauses the agent in < 100 ms with no stuck keys, (b) the hotkey stops everything in < 200 ms even with the core deliberately hung, (c) a scripted attempt to write into `C:\Windows` is denied and logged, (d) an approval left untouched for 30 s denies itself.

---

## 6. P4 — The agent loop

| ID    | Task                                                                                      | Blocked by          | Status | Notes                                               |
| ----- | ----------------------------------------------------------------------------------------- | ------------------- | ------ | --------------------------------------------------- |
| P4-01 | Tool registry: name, schema, risk,`undo()`, `describe()` — all five enforced by test | P3-08               | TODO   | **From P3-08:** a sixth required member: `targets(params) -> Sequence[guardian.rules.Target]` — every path/command/registry key the call touches, `read` or `write` (unsure = `write`). `guardian.policy.GuardedTool` is the protocol **From P3-12:** the approval dialog's banner reads `reversible`, which the loop passes to `ApprovalBroker.request(reversible=…)` — true only for a tool with a real `undo()` |
| P4-02 | Loop skeleton: perceive → plan → propose → guardian → execute → observe              | P4-01, P2-*, P3-* | TODO   | One action per turn, no batching. **Must publish `task.status` `{status: TaskState}` on every transition** — MAIN's watchdog (P3-07) acts only on `RUNNING`, and the health route must stay answerable while a step runs (perception off the event loop), or a slow step reads as a hang **From P3-03:** build the `InputController` with `motion=HUMAN` (`actuation/motion.py`); without it every move is a jump and hover-driven UI misbehaves **From P3-11:** build `TaskContext(task_id=…, allow_rules=broker.rules())`; for a `confirm`, `await app.state.approvals.request(tool, params, verdict, prompt=tool.describe(params), why=…, task_id=…)` and act only if `resolution.allowed`; write the `approvals` row (`prompt`, `choice`, `remembered` = a rule was made, `decided_at`) and `steps.approved_by` (`decided_by`) from the `Resolution`; call `deny_all()` when the task stops |
| P4-03 | System prompt + tool-schema serialisation per provider                                    | P1-08, P4-01        | TODO   | Keep prompts in versioned files, not inline strings |
| P4-04 | Context assembly (`ARCHITECTURE.md § 6.2`) with `UTILITY`-model step compression     | P1-08               | TODO   | One image per call, max                             |
| P4-05 | Verification step: did the expected change occur?                                         | P2-09, P4-02        | TODO   | Unverifiable ≠ successful                          |
| P4-06 | Stuck detector (3 near-identical observations + unchanged plan)                           | P2-09, P4-02        | TODO   |                                                     |
| P4-07 | Step + cost budgets, graceful stop-and-report                                             | P1-09, P4-02        | TODO   |                                                     |
| P4-08 | Cancellation/pause/resume plumbed through every phase and long action                     | P4-02               | TODO   |                                                     |
| P4-09 | Mid-run instruction injection (`POST /tasks/{id}/message`)                              | P4-02               | TODO   | Pairs with P3-14                                    |
| P4-10 | `ask_user`, `finish`, `give_up`, `remember_fact` task tools + the `facts` table | P4-01               | TODO   |                                                     |
| P4-11 | `StepCard`, `TimelineList`, `LiveView` to `UI.md § 4.2/4.3`                      | P0-09               | TODO   | Streaming thought text                              |
| P4-12 | **Benchmark suite**: 10 scripted real tasks with pass criteria, runnable headlessly | P4-02               | TODO   | This is how we measure "is it actually good"        |

**Gate:** ≥ 8/10 benchmark tasks complete without human help under Standard autonomy; every run has a full, replayable timeline.

---

## 7. P5 — Tool families

| ID    | Task                                                                                      | Blocked by   | Status | Notes                                      |
| ----- | ----------------------------------------------------------------------------------------- | ------------ | ------ | ------------------------------------------ |
| P5-01 | `screen`: observe, wait_for, read_text                                                  | P2-*         | TODO   |                                            |
| P5-02 | `input`: click, double, right, drag, type_text, press_keys, scroll                      | P3-01        | TODO   |                                            |
| P5-03 | `window`: list, focus, move_resize, close                                               | P2-03        | TODO   |                                            |
| P5-04 | `app`: launch (allowlisted + user-approved), is_running                                 | P3-08        | TODO   |                                            |
| P5-05 | `fs` read family: list_dir, read_file (text + pdf/docx extraction)                      | P3-10        | TODO   | Scope-enforced **From P3-10:** call `Guardian.check_target()` for every path immediately before the operation and act on the canonical path it returns, never on the param string |
| P5-06 | `fs` write family: write, move, copy, mkdir, zip/unzip                                  | P3-10, P6-01 | TODO   | Journalled before execution                |
| P5-07 | `fs.delete` → shadow copy to Aegis trash, never a real delete                          | P6-02        | TODO   | Security-gated                             |
| P5-08 | `shell.run_powershell` in a low-integrity child, scrubbed env, hard timeout, output cap | P3-08        | TODO   | Security-gated. Always DANGEROUS. **From P3-06:** every job must run inside a **Job Object** with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (and no breakaway), because the kill switch terminates the core and relies on the jobs dying with it. MAIN does not walk the process tree |
| P5-09 | Command classifier: block the destructive-verb list before it reaches PowerShell          | P5-08        | TODO   | Defence in depth, not the only defence **From P3-09:** the Guardian's FORBIDDEN command rules already match the text as written and with `^`/backticks/quotes removed; they do **not** resolve paths inside a command (`Remove-Item $env:LOCALAPPDATA\Aegis`). That, and verbs on paths, is this classifier's job |
| P5-10 | `browser`: Playwright with a dedicated profile — open, goto, find, click, extract      | P3-08        | TODO   | Never reuse the user's real Chrome profile |
| P5-11 | `browser.fill_web` — DANGEROUS tier, never fills password/payment fields               | P5-10        | TODO   | Security-gated                             |
| P5-12 | `clipboard` read/write with redaction on read                                           | P2-05        | TODO   |                                            |
| P5-13 | Egress allowlist enforcement for the whole core process                                   | P3-08        | TODO   | Security-gated                             |

**Gate:** each tool has params validation, a risk tier, an `undo()` or an explicit `undoable = False`, a `describe()` string a non-technical person understands, and a test proving Guardian blocks its out-of-scope form.

---

## 8. P6 — Recovery, undo, audit

| ID    | Task                                                                                                                                                                                                                  | Blocked by   | Status | Notes                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P6-01 | Action journal: write intent + undo payload**before** execution                                                                                                                                                 | P4-01        | TODO   |                                                                                                                                                                                                             |
| P6-02 | Shadow-copy store (`%LOCALAPPDATA%\Aegis\trash`) with retention + size cap                                                                                                                                          | P6-01        | TODO   |                                                                                                                                                                                                             |
| P6-03 | `undo(journal_id)` and `undo_last_n` incl. compound file operations                                                                                                                                               | P6-01, P6-02 | TODO   |                                                                                                                                                                                                             |
| P6-04 | Hash-chained audit log +`aegis verify-log`                                                                                                                                                                          | P0-10        | TODO   | Security-gated                                                                                                                                                                                              |
| P6-05 | Crash-safe task state: resume or cleanly abandon on restart                                                                                                                                                           | P0-10        | TODO   | Per`RECOVERY.md § 3`                                                                                                                                                                                     |
| P6-06 | Event replay from SQLite on WS reconnect (`?since=seq`)                                                                                                                                                             | P0-08, P0-10 | TODO   |                                                                                                                                                                                                             |
| P6-07 | VSS restore point before high-risk batches (best-effort, non-blocking)                                                                                                                                                | P5-06        | TODO   |                                                                                                                                                                                                             |
| P6-08 | Signed task report export (`.zip`: steps, screenshots, audit slice)                                                                                                                                                 | P6-04        | TODO   |                                                                                                                                                                                                             |
| P6-09 | Logs screen +`Verify log integrity` button (`UI.md § 8.5`)                                                                                                                                                       | P6-04, P0-09 | TODO   |                                                                                                                                                                                                             |
| P6-10 | `aegis.db` protection per `RECOVERY.md § 5`: backup copy on every start (keep 5), `PRAGMA quick_check` on open, corrupt DB → rename to `aegis.db.corrupt-<ts>` and start fresh, tell the user where it went | P0-10, P0-17 | TODO   | Found during P0-10. Today`bootstrap()` raises `StorageError` on an unreadable DB, the core exits 4, and after 3 attempts the supervisor gives up — so one corrupt file bricks the app until this lands |

**Gate:** kill the app mid-task → restart shows the correct state and offers undo; a manually edited audit row makes verification fail.

---

## 9. P7 — Packaging & installer

| ID    | Task                                                                                   | Blocked by | Status | Notes                                        |
| ----- | -------------------------------------------------------------------------------------- | ---------- | ------ | -------------------------------------------- |
| P7-01 | `build-core.ps1`: PyInstaller onedir, hidden imports resolved, size audit            | P0-05      | TODO   | onedir,**not** onefile. `comtypes` (P2-03) generates `comtypes.gen.UIAutomationClient` at first use — pre-generate it and bundle it as a hidden import, or the installed app writes Python into a cache dir on the user's machine **From P3-08:** bundle `aegis_core/guardian/rules.yaml` as data — the Guardian refuses to start without it (digest-pinned) |
| P7-02 | electron-builder config: NSIS, per-user, no admin, shortcuts, extraResources           | P7-01      | TODO   | **From P3-15:** `koffi` is a native module MAIN loads at runtime — add it to `asarUnpack` and check its `.node` is signed with the rest |
| P7-03 | Code signing (`sign.ps1`) for exe, sidecar, installer                                | P7-02      | TODO   | Blocks release                               |
| P7-04 | Auto-update via electron-updater + static feed + signature verification                | P7-03      | TODO   |                                              |
| P7-05 | Clean-VM install test matrix: Win 10 22H2 + Win 11, fresh user, no Python, no Node     | P7-02      | TODO   | Must pass before every release               |
| P7-06 | Uninstaller: remove app, offer keep-or-delete for user data                            | P7-02      | TODO   |                                              |
| P7-07 | First-run wizard (`UI.md § 8.7`) including the practise-the-kill-switch step        | P3-14      | TODO   |                                              |
| P7-08 | Portable`.zip` build                                                                 | P7-02      | TODO   |                                              |
| P7-09 | AV/SmartScreen false-positive submissions to Microsoft + major vendors                 | P7-03      | TODO   | **Start this early — it takes weeks** |
| P7-10 | Licensing scaffold: Ed25519 offline verify,`features.enabled()` gates returning True | P0-05      | TODO   | No billing in v1                             |

**Gate:** a fresh Windows VM with nothing installed runs `Setup.exe`, completes the wizard, finishes a real task, and takes an auto-update — with no SmartScreen warning.

---

## 10. P8 — Hardening & polish

| ID    | Task                                                                                             | Blocked by   | Status | Notes                     |
| ----- | ------------------------------------------------------------------------------------------------ | ------------ | ------ | ------------------------- |
| P8-01 | Prompt-injection test suite: screens/pages that try to hijack the agent                          | P4-02        | TODO   | Row 1 of the threat model |
| P8-02 | Injection mitigation: observation text framed as untrusted; echoed params escalate to`confirm` | P8-01        | TODO   | Security-gated            |
| P8-03 | Full threat-model test pass (`ARCHITECTURE.md § 8.6`, one test per row)                       | P8-02        | TODO   |                           |
| P8-04 | Performance pass against every target in`ARCHITECTURE.md § 13`                                | P7-05        | TODO   | Includes the opt-in live capture benchmark: `AEGIS_PERF=1` + `pnpm py:test` on a quiet machine (P2-10) |
| P8-05 | Accessibility pass: keyboard-only run, contrast audit, screen-reader check                       | P4-11        | TODO   |                           |
| P8-06 | Error-state pass: every row of`UI.md § 9` implemented and screenshot-tested                   | P4-11        | TODO   |                           |
| P8-07 | Docs: README, user guide, security whitepaper (the sales asset), privacy statement               | P7-05        | TODO   |                           |
| P8-08 | Beta with 10 real users; triage log; ship-or-fix decision                                        | P8-03..P8-07 | TODO   |                           |

**Gate:** no open security-gated issue; all perf targets met; 10 beta users, zero data-loss incidents.

---

## 11. Parallelisation guide

If more than one agent/session is working at once, these tracks barely touch:

- **Track A (core/agent):** P1 → P2 → P4 → P5
- **Track B (safety):** P3-04 … P3-11 → P6 → P8-01..03
- **Track C (UI):** P0-03, P0-09 → P3-12/13/14 → P4-11 → P8-05/06
- **Track D (release):** P7-01..P7-09 — can start as soon as P0 is done; **start P7-09 in week one**

Shared files that need care: `tools/registry.py`, `server/schemas.py`, `packages/shared/src/api.ts`.

---

## 12. Session log

Append one line per session. Newest at the bottom.

```
| Date | Session/agent | Tasks touched | Outcome | Next |
|------|---------------|---------------|---------|------|
```

| Date       | Session     | Tasks touched                      | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Next                                                                                                                                                                                         |
| ---------- | ----------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-05 | planning    | —                                 | Doc set created (ARCHITECTURE, UI, PROGRESS, REVIEW, RECOVERY, REMEMBER)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Start P0-01                                                                                                                                                                                  |
| 2026-09-07 | claude-code | P0-01                              | Monorepo scaffold: pnpm workspaces + turbo, tsconfig base, apps/desktop (Electron), apps/renderer (Vite+React+Tailwind v4), packages/shared + ui, core/ (ruff + mypy strict + pytest).`pnpm install && pnpm build`, eslint, tsc, ruff, mypy, pytest all green                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P0-02 Electron MAIN skeleton                                                                                                                                                                 |
| 2026-09-07 | claude-code | P0-02, P0-12                       | MAIN skeleton: frameless 1100×720 window, navigation locked down, tray (status / stop / show-hide / quit), single-instance lock. Preload moved to`bridge.cts` so it emits CJS. Vitest added — `pnpm test` ran **zero** tests before and now runs 10 TS + 2 pytest. Launched by hand: one window, second instance exits without spawning, close → 0 orphan processes. Logged P0-12 for the broken root `clean`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P0-03 renderer skeleton                                                                                                                                                                      |
| 2026-09-09 | claude-code | P0-03, P0-13, P0-14                | Renderer skeleton: both`UI.md § 2` palettes as explicit blocks (dark default, OS picks light, `data-theme` overrides) with the light values derived and contrast-checked; Tailwind's stock colour/size/radius scales cleared so an off-token class no longer compiles; shadcn foundation (`components.json`, `cn` over `extendTailwindMerge`, `Button`); `AppShell` to the `UI.md § 4` layout — 44px titlebar with a drag region, 64px rail, 360px live view, full-width composer disabled with "Connect a model to get started." Vitest + RTL wired into `apps/renderer` (35 tests, incl. one that fails if a token is dropped or defined for only one theme). Built app launched by hand: layout matches the sketch, rail switches sections, close → 0 orphan processes. Logged P0-13 (Storybook) and P0-14 (bundle the fonts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P0-04 preload bridge                                                                                                                                                                         |
| 2026-09-09 | claude-code | P3-04, P3-05, P3-02, part of P3-01 | Preemption spine.`actuation/win32.py` (ctypes bindings), `signature.py` (`AEGIS_SIGNATURE`), `input.py` (`SendInputBackend` — the only `SendInput` caller — plus `InputController` with a held-key registry and abort-aware pacing), `preempt.py` (`PreemptSignal` + `InputMonitor`: both LL hooks on one thread that does no I/O). 30 new tests. Measured on this machine: hook callback sets the signal in **1.06 ms median / 3.5 ms worst** (n=25); real keystroke → action aborted → all eight modifiers released in **6.3 ms median / 9.3 ms worst** (n=10). Suite run 5× consecutively, no flakes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P3-06 kill switch                                                                                                                                                                            |
| 2026-09-09 | claude-code | P0-04, P0-15                       | Preload bridge.`packages/shared/src/bridge.ts` holds the `AegisBridge` contract (all six `ARCHITECTURE.md § 9.3` namespaces); `preload/bridge.cts` exposes it over one named channel per method with no generic `invoke`; `main/bridge-channels.ts` lists every channel; `main/bridge-handlers.ts` is electron-free and does all validation; `main/ipc.ts` is the only file touching `ipcMain`/`dialog`/`shell` and adds a trusted-sender check plus `dispose()`. Calls resolve a `BridgeResult` and never reject, so `unavailable` (core/hotkeys/updates/overlay not built yet) is distinguishable from `failed`. `system.openPath`/`revealInExplorer` are gated by `main/path-grants.ts`: only paths that *resolve* (realpath — junctions, 8.3 names, `..`) inside a user-picked folder or an Aegis data dir, and `openPath` refuses executables outright. Titlebar minimise/close wired. 39 new tests (67 desktop, 37 renderer, 32 pytest, all green). Verified in real Electron with a throwaway harness against the compiled preload: exactly six namespaces with exactly the documented members, `C:\Windows\System32\cmd.exe` denied `not_granted`, `http://evil/` denied `invalid_request`, logs path revealed OK, minimise and close both working. Two notes for the next session — `apps/desktop` had to drop `verbatimModuleSyntax` (Decision Log) and **`ELECTRON_RUN_AS_NODE=1` in the agent terminal stops Electron launching at all**; see the new `RECOVERY.md § 6.3.1`. Logged P0-15 (maximise)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P0-05 Python core skeleton                                                                                                                                                                   |
| 2026-09-09 | claude-code | P0-05                              | Python core skeleton.`server/app.py` is a `create_app()` factory (no module-level singleton, so every test gets its own uptime clock) with a lifespan that logs `core.started`/`core.stopped`; `server/routes.py` mounts `/v1` and answers `GET /v1/health` with `{status, version, uptime}` exactly as `ARCHITECTURE.md § 9.1` specifies; `server/schemas.py` holds `HealthResponse` (frozen, `extra="forbid"`) and is the source the P0-11 TS generator will read. `logging_setup.py` writes one JSON object per line to a **rotating** `%LOCALAPPDATA%\Aegis\logs\core.log` (5 MB × 5 — `REVIEW.md § 2` forbids an unbounded log) and to **stderr**, never stdout (Decision Log); `configure_logging()` is idempotent, and `extra={...}` fields merge into the payload without being able to overwrite the level, logger or timestamp. `__main__.py` parses `--port` and serves on `127.0.0.1` with `log_config=None` so uvicorn's own lines flow through the same formatter. Deps added: fastapi, uvicorn, pydantic (+ dev `httpx2`, which starlette's TestClient now requires; one targeted `filterwarnings` ignore for a deprecation raised inside starlette itself). 28 new tests (60 pytest, 67 desktop, 37 renderer, all green); ruff + `mypy --strict` clean. Exercised by hand: server up, `/v1/health` 200 with a growing uptime, `/health` and `/openapi.json` 404, log file and stderr both filling, **0 bytes on stdout**. **Not** in this task, by design: the § 3.1 handshake, bearer auth and the peer-PID check — all P0-06, which is why the port must be passed explicitly for now                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P0-06 startup handshake                                                                                                                                                                      |
| 2026-09-09 | claude-code | P0-06, P0-16                       | Startup handshake, both halves.**MAIN:** `main/handshake.ts` mints a 256-bit token, spawns the core, writes the token to the **stdin pipe** and closes it, then reads exactly one JSON line off stdout — with a chunk-boundary-safe reader, an 8 KiB cap, a 15 s timeout, and `child.kill()` on *every* failure path so a bad greeting never leaves a process with mouse control running. `main/core-gateway.ts` is now the only thing that speaks HTTP to the core: it adds the origin, port, `/v1` prefix and bearer token, sends no `Origin`, times out every call, refuses redirects, caps the response, and reports transport errors without echoing anything that could carry the token. **Core:** `server/handshake.py` (`read_token` — a missing token is fatal, since a bound port with no credential must never exist; `bind_loopback` — the socket is bound *before* uvicorn so the ephemeral port can be announced; `announce` — one line, then stdout is closed and `sys.stdout` repointed at a sink so a stray `print` can neither corrupt the channel nor crash the core). `server/auth.py` is a pure-ASGI `SessionAuthMiddleware`: `Origin` present → reject, then a constant-time bearer compare, then the psutil peer-PID check, all answering one identical bare `401` so a prober cannot learn which check it tripped. `create_app()` now *requires* a `SessionAuth` and the middleware wraps FastAPI's own 404s. **The bug the harness caught:** `os.getppid()` in the core does not name MAIN — the venv `python.exe` re-execs, so MAIN is a *grandparent* — and the peer check refused MAIN itself. MAIN now passes `--supervisor-pid` explicitly (Decision Log). 64 new tests (112 desktop, 37 renderer, 120 pytest, all green); ruff, `mypy --strict`, eslint, tsc clean. Exercised by hand against the real core with a throwaway harness: handshake in ~900 ms, authorised `GET /v1/health` → 200; no token, wrong token, right-token-plus-`Origin`, and an unknown route unauthenticated → all `401` with byte-identical bodies; an unknown route *with* the token → 404; **a separate process holding the correct token → 401** (`untrusted_peer`); **0 bytes** on stdout after the handshake line; no orphan core after the harness exits. Built Electron app launched by hand — unchanged, 0 orphans. Logged P0-16: `§ 3.1`'s "child chain" also admits the jobs the *core* spawns, which must be narrowed before P5-08                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P0-07 supervisor: health check, 3-strike respawn, kill-on-exit, and wiring`rest.core` into `index.ts`                                                                                    |
| 2026-09-10 | claude-code | P0-07, P0-17, P3-15                | Supervisor, both halves.**MAIN:** `main/supervisor.ts` is the lifecycle around `handshake.ts` — it health-checks `GET /v1/health` the moment the core announces a port (two tries; anything but a 200 kills that core), restarts a core that dies on its own, allows **3 attempts inside a 60 s window** and then settles on `unavailable` instead of spawning forever, and kills the core on `stop()` with a SIGTERM→SIGKILL escalation. `gateway()` is the live core or `null`, which is what `rest.core` now returns in `index.ts`, so the renderer keeps seeing an honest `unavailable` while the core is down. `resolveCoreLaunch` resolves the packaged `resources/core/aegis-core.exe` or the dev `core/.venv/Scripts/python.exe -m aegis_core`, with `AEGIS_CORE_COMMAND` overriding both. **Core:** `aegis_core/parent_watch.py` polls the supervising PID every 0.5 s on a daemon thread, identifies it by **PID + creation time** so a reused PID cannot pass for MAIN, treats `AccessDenied` as alive (only a death stops the core), and on loss asks uvicorn to stop with a 1 s hard-exit backstop — 2 s budget, met with room. 26 new tests (142 desktop, 37 renderer, 132 pytest, all green); ruff, `mypy --strict`, eslint, tsc, prettier clean. Exercised by hand on the **real built app**: core spawned with `--supervisor-pid`, `/v1/health` 200, MAIN force-killed → core gone in **0.20 s** with 0 orphans, graceful quit → **0.33 s**; and the parent watch on its own, with a supervisor whose death does not touch the core's pipes → **0.71 s** and a `core.supervisor_lost` log line. (`RECOVERY.md § 6.3.1` again: `ELECTRON_RUN_AS_NODE=1` in the agent terminal makes Electron refuse to launch — clear it first.) Logged P0-17 (the Engine-unavailable screen has no renderer yet) and P3-15 (`RECOVERY.md § 4` wants MAIN to release held modifiers when the core dies, and MAIN has no input path until P3-06)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P0-08 WebSocket event hub                                                                                                                                                                    |
| 2026-09-13 | claude-code | P0-08                              | WebSocket event hub, core side.`server/hub.py`: `EventHub.publish()` is callable from any thread (the agent loop and preemption watcher are not on the event loop), stamps `seq` under an `RLock` and fans out with `call_soon_threadsafe`, so it never waits on a subscriber. Replay comes from a 2048-event in-memory buffer. Subscribing snapshots the backlog and registers under the same lock, so nothing is missed or duplicated at the boundary. A `since` that is evicted, or ahead of this core, is refused with `4410` rather than replayed with a hole. A subscriber more than 512 live events behind is cut off with `4429` and catches up with `since`. `WS /v1/stream` in `server/routes.py` accepts before refusing so the client sees a close code; malformed `since` → `4400`, a client message → `1003` (the stream is one-way). Reader and writer run in an **anyio task group**: the first draft used bare `asyncio` tasks and failed intermittently when the handler was cancelled, and a real server shutdown takes the same path. Deps: `websockets` (uvicorn cannot upgrade without it) and `anyio` made explicit. 34 new tests (166 pytest, 127 desktop, 37 renderer, all green; the core suite run 3× with no flakes); ruff, `mypy --strict` clean. One of them runs real uvicorn on a loopback socket with a cross-thread publisher, because TestClient would not notice a missing WS library. Test-only gotcha: starlette's `websocket_connect` **writes into the headers dict you pass it**, so a shared `AUTHORIZED` constant broke later tests. Exercised by hand against the real core via the § 3.1 handshake: authed connect opens; `since=5` on a fresh core → `4410`; `since=abc` → `4400`; no token and `Origin` → HTTP 403; client message → `1003`; core gone after the harness ends. Contract written into `ARCHITECTURE.md § 9.2`. Not done: nothing publishes events yet, and MAIN has no WS client (moved into the P0-09 note). Desktop shows 127 tests where the P0-07 line says 142. No TS was touched this session; worth a look                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P0-09 renderer event-stream client (incl. MAIN's WS client)                                                                                                                                  |
| 2026-09-14 | claude-code | P0-09                              | Event stream end to end: core → MAIN → renderer store → titlebar.**MAIN:** `main/core-stream.ts` owns the WebSocket, because the token must stay in MAIN and Electron 32's Node has no usable `WebSocket`, hence `ws`. It tracks the last `seq` forwarded and reconnects to the same core with `?since=` (250 ms → 5 s backoff), dropping duplicates across the boundary. When the supervisor reports a *different* core (`supervisor.onSession`, new), on close `4410`/`4400`/`1003`, and on every renderer `did-finish-load`, it drops the cursor, sends `reset` and replays from scratch. It announces `connecting`/`live`/`down`/`unavailable`, taking `down` vs `unavailable` from supervisor state. An unreadable frame is dropped and only its size is logged. **Contract:** `CoreStreamMessage` (`event`/`reset`/`connection`) over the existing `aegis:core:event` channel. No bridge member was added; the § 5 walk is in the P0-09 row. **Renderer:** `lib/stream-event.ts` validates every message (the payloads carry scraped screen text). `stores/stream.ts` is a pure `reduceStream` behind Zustand: connection, `hasBeenLive`, `lastSeq`, the last 1000 events, a rejected count; out-of-order or duplicate `seq` is ignored, and `reset` keeps connection state. `connectStream()` runs in `main.tsx` before the first render. The titlebar `EngineStatus` shows *Starting the engine…* / *Reconnecting…* / a red *The engine is not running.* (`UI.md § 4.1` updated). `StreamEvent` types are hand-seeded in `shared/api.ts` for P0-11 to replace (Decision Log). Deps: `ws`, `@types/ws`, `zustand`. 18 desktop tests (incl. a real `ws` server checking the token is sent and no `Origin` is) and 31 renderer tests: 145 desktop, 68 renderer, 166 pytest, all green; tsc, eslint (0 warnings), prettier clean. **Exercised on the real built app** over CDP: *Starting the engine…* → idle 1.1 s after first paint; core force-killed → *Reconnecting…* immediately → respawned core (new PIDs) → idle 1.2 s later; page reload → reset → idle in 0.3 s; quit → 0 orphan cores. Not shown by hand: events actually flowing in Electron, because nothing in the core publishes yet (covered by the real-socket tests on both sides). Gotcha for the next session: Python `write_text` on Windows writes CRLF, which prettier flags; write bytes or run prettier after                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P0-10 SQLite bootstrap                                                                                                                                                                       |
| 2026-09-14 | claude-code | P0-10, P6-10                       | SQLite bootstrap.`core/aegis_core/storage/db.py`: `connect()` creates the folder and opens `%LOCALAPPDATA%\Aegis\aegis.db` in autocommit mode, **verifies** WAL rather than assuming it, sets `synchronous=FULL` (a journal row a power cut can revoke is not write-ahead), foreign keys on, `trusted_schema=OFF`, a 5 s busy timeout, and refuses SQLite < 3.38 (`STRICT` + `json_valid`). `migrate()` versions by `PRAGMA user_version` and runs `BEGIN IMMEDIATE; <sql>; PRAGMA user_version=N; COMMIT` as one script, because `executescript` silently commits a transaction opened before it. It rolls back and raises on failure, tolerates another process having applied the same step, and refuses a DB newer than the build and a gapped or duplicated migration list. Migrations are **Python modules listed by hand** in `migrations/__init__.py`, not discovered `.sql` files, so PyInstaller cannot drop one. `m0001_initial.py`: the eight § 7 tables as `STRICT`, with `CHECK`s on task status, risk tier, Guardian decision, approval choice, 0/1 booleans, JSON validity and 64-char hashes; `choice`/`decided_at` set together; unique `(task_id, idx)` and `(scope, key)`; cascades from `tasks`; `audit` UPDATE/DELETE blocked by trigger. `journal` gained `forward_json` + `created_at` from `RECOVERY.md § 2.1`, and `cost_cents` is `REAL` (Decision Log; § 7 updated). `__main__.py` calls `bootstrap()` after reading the token and before binding, so a DB the core cannot use is a failed start (exit 4, `core.storage_failed`) rather than a failed task. 31 new tests (197 pytest; desktop/renderer untouched); ruff, `mypy --strict` clean. Exercised on the **real built app**: fresh profile → `aegis.db` created, `journal_mode=wal`, `user_version=1`, all eight tables, `storage.migrated` + `storage.ready` logged; force-killed and relaunched on the same DB → `storage.ready` only (no re-migration), core up; 0 orphan cores both times. Gotcha: `ruff format aegis_core` reformats `actuation/input.py` on its own — scope the formatter to the files you touched. Logged P6-10 (a corrupt DB currently bricks startup)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P0-11 Pydantic→TS type generation                                                                                                                                                           |
| 2026-09-14 | claude-code | P0-11, P0-18                       | Pydantic→TS type generation.**Core:** `server/typegen.py` renders `schemas.py` by introspection rather than JSON Schema, so `EventType` keeps its name (Decision Log). `BaseModel`s defined in the module → `readonly` interfaces; `Literal` aliases → `const NAME_S = [...] as const` + `(typeof NAME_S)[number]`; docstrings, field descriptions and string literals after an alias → JSDoc (`*/` escaped); `str`/`int`/`float`/`bool`/`None`/`Literal`/unions/`list`/`dict[str, T]`/`JsonValue`/sibling models are supported, and anything else, an imported model or a non-identifier alias raises `TypeGenError` naming the field. `main()` writes UTF-8 + LF bytes to stdout (Windows text-mode `print` would write CRLF). `RiskTier` moved into `schemas.py`, and the `#:` comment on `EventType` became an attribute docstring. **Script:** `scripts/gen-types.ts` runs `python -m aegis_core.server.typegen` from `core/.venv` with a 60 s timeout, formats it with the repo prettier config and writes `api.ts`, or with `--check` exits 1 if it differs. It runs on Node's built-in type stripping (`engines` → `>=22.18.0`, root `"type": "module"`, `scripts/tsconfig.json` with `erasableSyntaxOnly`, `@types/node` at root, `pnpm typecheck` now includes it). Wiring: `pnpm gen:types`, `pnpm gen:types:check`; `pnpm dev` generates first; `pnpm test` checks first. `api.ts` is now fully generated: same names and shapes for `EVENT_TYPES`/`EventType`/`StreamEvent`/`RiskTier` (plus a `RISK_TIERS` tuple), and `HealthResponse` is new. 16 new pytest tests (213 pytest, 145 desktop, 68 renderer, all green); ruff, `mypy --strict`, tsc (incl. scripts), eslint, prettier clean. Exercised: the committed file passes `--check`; adding a field to `HealthResponse` makes `pnpm test` fail with the stale message and reverting makes it pass. The **built app** launched: core spawned, `/v1/health` 200, the renderer's stream connected (`stream.connected` logged) on the generated `EVENT_TYPES`; Electron force-killed → 0 orphan cores. Logged P0-18: the task line says "CI fails" and there is no CI workflow in the repo. Gotcha: the Edit tool treats a JSON `\u2014` escape and a literal em dash as the same string, so restoring one needs a byte-level write                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P0-12 root`clean` script                                                                                                                                                                   |
| 2026-09-14 | claude-code | P0-12                              | Root`clean` fixed. `rimraf node_modules` deleted the rimraf it ran from. It is now `scripts/clean.ts`, which uses only Node built-ins and removes root + workspace `node_modules` (`WORKSPACE_DIRS`, checked against `pnpm-workspace.yaml`) with `rmSync` (`maxRetries` for Windows AV locks). pnpm's workspace junctions are unlinked, not followed. A test builds a real junction to a package's `src` and asserts the source survives. Root `rimraf` devDependency removed (lockfile: that importer entry only). Test wiring: `pnpm scripts:test` (`node --test scripts/clean.test.ts`) runs inside `pnpm test`; `scripts/tsconfig.json` gained `allowImportingTsExtensions` so the test can import `./clean.ts` (it is `noEmit`). 4 new tests (145 desktop, 68 renderer, 4 scripts, 213 pytest, all green); tsc, eslint, prettier clean. **Exercised on this repo:** `pnpm clean` → exit 0, five `node_modules` removed, `git status` unchanged; `pnpm install && pnpm build && pnpm test` green again; the rebuilt app launched, core `/v1/health` 200 and `stream.connected`, Electron force-killed → 0 orphan cores. Gotcha: `pnpm remove`/`add` rewrites the `—` escape in root `package.json` to a literal em dash; PowerShell `.Replace` did not restore it, a small Node script did                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P0-13 Storybook                                                                                                                                                                              |
| 2026-09-15 | claude-code | P0-13                              | **Inherited WIP** from an uncommitted 2026-09-14 session (config, bridge stub, stories, coverage test all on disk, never committed or logged). Verified it per `RECOVERY.md § 6.2` rather than rebuilding: `pnpm install && pnpm build && pnpm test` green (98 renderer incl. 30 new story tests, 145 desktop, 4 scripts, 213 pytest); eslint (now also `.storybook`), tsc, prettier clean; `pnpm build-storybook` succeeds with all 9 stories in `index.json`. **Exercised by hand:** served `storybook-static` on loopback and screenshotted stories in headless Edge. AppShell Error (dark) shows the red dot and *The engine is not running.*, LongestContent (light) fits 880×600, Button Variants renders all four on tokens. The Electron app was not relaunched, because no runtime renderer or MAIN code changed (devDependencies, stories, tests and config only; renderer bundle unchanged at 193 kB). Recorded the enforcement convention in `UI.md § 12` and two Decision Log rows (test-enforced states; `notApplicable` with a reason, never for `Error` on inventory components). Note: Storybook's build warns about a >500 kB chunk. That is its own manager bundle, dev-only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P0-14 bundle fonts                                                                                                                                                                           |
| 2026-09-15 | claude-code | P0-14                              | Bundled fonts.`@fontsource-variable/inter` and `/jetbrains-mono` (5.3.0, exact) are imported in `index.css`; the type stacks now lead with `Inter Variable` / `JetBrains Mono Variable`. **Two defects found by hand, both fixed and tested:** (1) clearing `--font-*` had also dropped Preflight's `--default-*-font-family`, so bare `<code>`/`<pre>` would have fallen to Consolas, and they now point at the tokens; (2) in real Electron the CSP refused a `data:font/woff2` URI, because Vite inlined JetBrains Mono's cyrillic-ext subset (< 4 KB). `assetsInlineLimit` now never inlines `.woff2`, and the CSP was **not** widened. `tests/fonts.test.ts` (12) checks the imports, the leading families, that every `@font-face` uses a bundled `./files/*.woff2` (no `local()`, no URL), the Preflight defaults, and `font-src` → `'self'`. It also runs a real in-memory `vite build` (~4 s) and asserts every face is emitted as a file with no `data:font`. With the config line removed, that test fails. 110 renderer (+12), 145 desktop, 4 scripts, 213 pytest, all green; eslint, tsc, prettier clean; `build-storybook` OK. **Exercised on the built app** over CDP (`file://`): `document.fonts.check` true for both families; latin, JetBrains cyrillic-ext and Inter greek-ext all `loaded`; zero console/CSP/network errors; screenshot shows Inter; force-killed → 0 orphan cores. Renderer build now emits 12 woff2 (~300 kB)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P0-15 maximise/restore                                                                                                                                                                       |
| 2026-09-16 | claude-code | P0-15                              | Maximise/restore, end to end. The surface grew by**two** members, not one: `window.maximize()` toggles, and `window.onMaximizedChange()` carries the state MAIN pushes — a frameless window is also maximised by double-clicking the drag region, by `Win`+`↑` and by Aero snap, so a `□` that tracked only its own clicks lies within a minute of use, and measuring the viewport instead would be a guess (Decision Log, `ARCHITECTURE.md § 9.3` + `UI.md § 4.1` updated). MAIN pushes on Electron's `maximize`/`unmaximize` and on `did-finish-load`, so a reloaded page starts from the truth. The toggle itself lives in the electron-free `bridge-handlers.ts` — `WindowService` is now `isMaximized` + `setMaximized`, and `ipc.ts` contributes two Electron calls and no logic — because `ipc.ts` has no unit tests and `REVIEW.md § 1` auto-rejects an untested preload change. Renderer: `stores/window.ts` (pushed, never polled) fed by `lib/window-client.ts` from `main.tsx` before the first render, and the control renders *Maximise* (`Square`) or *Restore* (`Copy`) — label **and** icon, per `REVIEW.md § 3`. Storybook: a `Maximised` story, the window store reset in `preview.tsx`, and the story bridge widened. 8 new tests (147 desktop, 116 renderer, 4 scripts, 213 pytest, all green); eslint, tsc, prettier, `build-storybook` clean; no Python was touched. **`REVIEW.md § 5` walked** — the new call takes **no argument at all**, so scraped screen text has nothing to influence; the payload is one boolean with no path, token or user data; the send goes through the same trusted-sender check as `minimize`/`close` and the push only ever reaches the window MAIN opened; no core, Guardian, scope or input path was touched. **Exercised on the real built app** over CDP against Win32 ground truth (`IsZoomed`): ten transitions — renderer click both ways, external `SW_MAXIMIZE`/`SW_RESTORE`/`SW_SHOWNORMAL`, minimise-and-restore, and a page reload while maximised — titlebar matched the window every time; force-killed → 0 orphan cores. Gotcha for the next session: reading `window.outerWidth` right after a maximise races the animation and reports nonsense (157 px once); settle ~1.5 s before believing a hand check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P0-17 Engine-unavailable screen                                                                                                                                                              |
| 2026-09-16 | claude-code | P0-17                              | Engine-unavailable screen, end to end.`EngineUnavailable` takes the whole conversation panel the moment the stream says `unavailable`, with `RECOVERY.md § 4`'s copy verbatim and its three buttons; the outcome of each is announced in an `aria-live="polite"` line, and every button is disabled while one is in flight. **Two** members were added to `§ 9.3`, both taking *no argument at all*: `core.restart()` and `app.copyDiagnosticReport()` — the launch spec, the attempt budget and the log file all stay in MAIN (Decision Log). `supervisor.restart()` kills the core that is there, **clears the 60 s attempt window** (a person asking is not the runaway-respawn case the 3-strike rule guards) and starts over; MAIN's `CoreControl.restart` **throws unless the core ends up `running`**, because resolving `ok` on a core that never came up reported success to a user still looking at a dead engine — the first live run did exactly that. `main/diagnostics.ts` builds the report and `ipc.ts` writes the clipboard **in MAIN**, so log text (paths today, scraped screen text from P2) never enters the renderer: home paths → `%USERPROFILE%\…`, `token`/`api_key`/`secret`/`password`/`authorization` values and 32+ char blobs → `[redacted]`, every line capped at 500 chars, last 200 lines tailed from at most 512 KiB. `Open logs` needed no new member, but `app.logsPath()` was pointing at Electron's `app.getPath('logs')` — an empty folder — while the core writes to `%LOCALAPPDATA%\Aegis\logs`; `main/paths.ts` mirrors the core's rule, and MAIN creates and grants that folder so the button works even when the core never started. The screen's `busy`/`notice` live in `stores/recovery.ts` rather than `useState`, so the required Loading and Error stories are real. 26 new tests (170 desktop, 142 renderer, 4 scripts, 213 pytest, all green); eslint, tsc, prettier, `build-storybook` clean; no Python touched. **`REVIEW.md § 5` walked** — neither new call takes an argument, so scraped screen text has nothing to influence; both go through the same trusted-sender check; the report is redacted **before** it leaves MAIN and a unit test fails if either rule is removed; no Guardian, scope, key or input path was touched, and the only widening (restart the core, copy a redacted report) is user-initiated and visible. **Exercised on the real built app** over CDP: killed the core until the supervisor gave up → screen appeared with the red dot; `Copy report` → 42 kB on the clipboard, 210 lines, 25 home paths replaced, **zero** occurrences of the developer's username; `Restart engine` → engine back and the screen gone in ~1 s; then relaunched with a deliberately broken `AEGIS_CORE_COMMAND` → restart failed and the screen came back with *The engine did not start.*; a `MutationObserver` caught the *Starting…*/all-disabled frame in between. Force-killed → 0 orphan cores. Gotcha: with a bogus command the three attempts take ~1 ms total (the retry delay only applies to a core that *had* started), so the busy state is a single frame — poll with a `MutationObserver`, not a timer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P0-16 narrow the peer-PID check (security-gated), then P0-18 CI                                                                                                                              |
| 2026-09-16 | claude-code | P0-18                              | CI.`.github/workflows/ci.yml` is one `windows-latest` job — Windows-only because the product is and because the core's actuation suite drives real Win32 hooks — running exactly the commands a developer runs: `pnpm install --frozen-lockfile`, a venv at `core/.venv` + `pip install -e '.[dev]'`, then `pnpm build`, `pnpm lint`, `pnpm typecheck`, `pnpm py:lint`, `pnpm py:typecheck`, `pnpm test`. There is deliberately **no** `py:test` step and no CI-only check: `pnpm test` already chains `gen:types:check` → every workspace suite → `scripts:test` → `pytest`, so the gate cannot exist in CI and be skippable locally (which is what P0-11 logged this task for). `permissions: contents: read`, `concurrency` cancels superseded runs, 30 min job timeout; Node 22.x with the pnpm store cached, Python 3.11. The venv path is not a preference — `py:lint`/`py:typecheck`/`py:test` and `scripts/gen-types.ts` all spell `core/.venv/Scripts/python.exe`. **The test for a CI file is a drift test**, because CI is the one check that cannot fail on the machine that edits it: `scripts/ci-workflow.test.ts` (7 `node:test` tests, wired into `pnpm scripts:test`) asserts it runs on Windows, installs frozen, runs only scripts the root `package.json` defines, runs all six required ones, builds the venv where the `py:*` scripts look, and pins a Node major matching `engines.node` and a Python matching `requires-python`'s lower bound. Mutation-checked: renaming `py:lint`, flipping to `ubuntu-latest` and dropping the `[dev]` extra each fail it (4 of 7 red). YAML validated by parsing it with PyYAML in a throwaway prefix. 7 new tests (11 scripts, 170 desktop, 142 renderer, 213 pytest, all green); eslint, tsc (incl. `scripts`), ruff, `mypy --strict`, prettier clean. The app was **not** relaunched: no MAIN, renderer, preload or core code was touched — the diff is the workflow, the new test and one line of root `package.json`. **Honest gap: the workflow has never run.** GitHub Actions cannot be executed from here, so the first push is the real test; watch in particular whether `windows-latest` gives the actuation hook tests an interactive desktop                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P1-01`ModelProvider` protocol. **P0-16 is still the only P0 open and is not pickable** — its blocker P5-08 is TODO, so the previous session's "next" was wrong                      |
| 2026-09-16 | claude-code | P0-16                              | Peer-PID check narrowed (security-gated). Taken**out of order at the user's request** — P5-08 is still `TODO`, but the rule is self-contained and P0-16's own note only required it to land before P5-08 ships. The peer must now be the supervising process **itself**, not anything in its child chain: the core is a descendant of MAIN, so a PowerShell or Playwright job the *core* spawns was inside that chain and would have been able to drive the agent through the agent's own API. `SessionAuth.is_the_supervisor(pid)` replaces `is_supervised_by`, the `psutil` ancestry walk and `_MAX_ANCESTRY_DEPTH` are gone, and the supervisor is identified by **PID + creation time** snapshotted in `__post_init__` — `identities_match` reuses `parent_watch.process_identity`, so PID reuse cannot pass for MAIN. `AccessDenied` falls back to PID equality rather than denying, for the same reason `parent_watch` treats it as alive: refusing would lock MAIN out of its own core on a permissions change, and the caller still needs the 256-bit token. Nothing needed the wider rule — MAIN is the only client, and `core-gateway.ts` (undici) and `core-stream.ts` (`ws`) both open their sockets in MAIN's own process. 7 new tests, 4 removed; the headline one spawns a **real** child and asserts it genuinely is below both the core and MAIN *before* asserting it is refused, so it cannot quietly stop covering the case. 216 pytest (+3), 170 desktop, 142 renderer, 11 scripts, all green, server suite run 3× with no flakes; ruff, `mypy --strict`, eslint, tsc, prettier clean; no TS was touched. **`REVIEW.md § 5` walked** — this only *narrows* what can reach the agent, and the one widening in it (`AccessDenied` → PID-only) is documented and tested; no token, key or path handling changed; the refusal is still the byte-identical bare `401`, so a prober learns nothing. **Exercised on the real built app**: `GET /v1/health` 200 and `WS /v1/stream` accepted from MAIN, core stable, MAIN force-killed → 0 orphans. **Then proved both ways against a real core** with a throwaway harness (MAIN → core → job, same valid token everywhere): new rule → MAIN `200`, job `401` + `core.peer_untrusted`; with the old `auth.py` stashed back in, that same job got **`200`**. `ARCHITECTURE.md § 3.1` step 5 and the `§ 8.6` threat-model row rewritten. **P0 is now 18/18 tasks but the phase gate is still unmet** — the composer round-trip needs a model, so P0 stays 🟨 until P1-08                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P1-01`ModelProvider` protocol + `ChatRequest`/`ChatDelta`/`Capabilities` schemas                                                                                                     |
| 2026-09-16 | claude-code | P1-01                              | The model layer's contract.`models/schemas.py` holds the shapes and `models/provider.py` the `ModelProvider` protocol; they are **internal**, so `server/schemas.py` and the generated `api.ts` are untouched (Decision Log). Three deviations from `ARCHITECTURE.md § 5.1`, all now written back into it: `chat` is `def … -> AsyncIterator`, not `async def`, because every adapter is an async generator and the `async def` spelling would mean no real adapter satisfied the protocol under `mypy --strict`; `capabilities()` takes a **model id**, since `openrouter` serves hundreds of models and vision belongs to the model, not the account; and `cost_per_mtok` is split input/output, which `P1-09` has to add separately. The error hierarchy ships here rather than with the first adapter, because `§ 5.3`'s "fall back on 429/5xx/timeout, never on a refusal" is a rule about error *types* — `ProviderTransientError` is the only `retryable` one, and a refusal is a successful call that finishes `content_filter`. Security choices, each with a test: `ChatRequest` has **no field a key could sit in** (asserted over `model_fields`), `ImagePart.data` is `repr=False` so the user's screen cannot reach a log line, and `ProviderError` carries provider + status + a short reason, never a response body. Everything is frozen, `extra="forbid"` and tuple-valued; `AssistantMessage` rejects a turn with neither text nor a tool call. 43 new tests (259 pytest, 170 desktop, 142 renderer, 11 scripts, all green); ruff, `mypy --strict`, eslint, tsc, prettier clean; `gen:types:check` still passes, which is the proof the TS surface did not move. **Exercised by hand in the real venv**: a fake adapter drove the whole protocol — `isinstance(ModelProvider)` true, a four-message request with an image round-tripped through JSON, the stream yielded text → tool_call → usage → done, abandoning it mid-stream ran the adapter's `finally` (the preemption path), and `repr()` of the request contained no image bytes. Then the **real core** was started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake in ~200 ms, `GET /v1/health` 200, **0 extra bytes** on stdout, core gone on terminate, 0 orphans. The Electron app was **not** relaunched — no MAIN, renderer, preload or core runtime code was touched, and nothing imports the new modules yet. Gotcha: ruff's isort puts `aegis_core` *before* `pydantic` in test files; let `ruff check --fix` decide rather than hand-sorting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P1-02`openai` adapter — the workhorse, five providers reuse it                                                                                                                            |
| 2026-09-17 | claude-code | P1-02                              | The`openai` adapter, and with it the shape the other four compatible providers will take. `OpenAIProvider` is one class over Chat Completions; everything provider-specific is a frozen `ProviderConfig`, so `P1-05`/`P1-06` add a config and no code (Decision Log — a subclass is where a bespoke client starts). Streaming text, tool calls, vision, JSON mode, stop sequences, `stream_options.include_usage`, and a cost in cents from the model table. `httpx2` was promoted from a dev to a **runtime** dependency: it is already in the tree, streams async, and its `MockTransport` makes every recorded-response test socket-free. **Three things found by building it, all now tested:** (1) wrapping only the SSE loop in the error translation left a *connect* failure escaping as a raw `httpx2.ReadTimeout` — the whole exchange, status line included, is now inside one translation block, because nothing but a `ProviderError` may leave an adapter; (2) an image in a tool result cannot be expressed in this API, and dropping it would answer a vision question with nothing, so it is a `ProviderCapabilityError`; (3) `httpx2.EventSource` iterates the response in a generator nothing outside it can close, so **abandoning a stream — the preemption path — logged `generator didn't stop after athrow()` on every stop**. The adapter now reads SSE itself (~25 lines, capped) and closes the reader before the response: measured on a real loopback server, one abandoned stream went from one logged error to zero. Security: the key is an injected `KeyLookup` read once per request and never stored (asserted over `vars(provider)`), errors carry a status and a short reason and **never a response body** (asserted with a key embedded in a fake 401 body), and the capability gate refuses before anything reaches the wire. 40 new tests (299 pytest, 170 desktop, 142 renderer, 11 scripts, all green) plus a live smoke test skipped without `AEGIS_OPENAI_TEST_KEY`; ruff, `mypy --strict`, eslint, tsc, prettier clean, `gen:types:check` still passes — these shapes are internal, so no TS moved. **Exercised by hand** against a real OpenAI-compatible server on loopback (not the mock transport): validate_key 200, text → assembled tool call → usage (`cost_cents` 0.045 for 1 M in + 0.5 M out at 0.15/0.60) → done, the `Authorization: Bearer` header seen by the server, the key read once per call, and a stream abandoned mid-flight closing silently. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 0.87 s, `GET /v1/health` 200, **0 extra bytes** on stdout, gone on terminate, 0 orphans — the dependency change is the reason that mattered. The Electron app was **not** relaunched: no MAIN, renderer, preload or core runtime code was touched and nothing imports the adapter yet. **Not** in this task: no live call to OpenAI itself (no key on this machine), so the wire format is proved against the recorded shape and a local server, not against the vendor — the first real key, or `P1-10`'s Test button, is the real proof. Gotcha for the next session: `pnpm` must be run from the repo root; the PowerShell tool keeps whatever directory the last command left it in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P1-03`anthropic` adapter                                                                                                                                                                   |
| 2026-09-17 | claude-code | P1-03                              | The`anthropic` adapter — the second and last *real* adapter, since the remaining four are `openai` with a different base URL. Before writing it, the SSE reader, status map, cost sum and client factory moved out of `openai.py` into `models/providers/common.py`: `P1-03` is the second caller, and the reason that reader does not use `httpx2.EventSource` is a subtle point about the preemption path that a copied version would lose the first time somebody tidied it (Decision Log). The move is mechanical and **`P1-02`'s 43 tests passed through it unchanged** — only three import lines in its test file moved with the constants. `AnthropicProvider` is a module rather than another `ProviderConfig` because four differences are not expressible as configuration: the system prompt is a **top-level field** with no wire role; a tool result is a block inside a **user** turn, so a step that ends with one produces two consecutive user turns that the API refuses — `_coalesce` merges them; `max_tokens` is **required**, so a request that names none gets `DEFAULT_MAX_TOKENS` rather than a 400; and the stream is named events whose deltas address a **block index**, with usage split between `message_start` and `message_delta`. Cached tokens are counted as input rather than dropped — the user was charged for them. **No JSON mode exists in this API**, so every model advertises `json_mode=False` and the gate refuses before the wire rather than letting the model answer prose that will not parse (Decision Log); a temperature above 1 is refused the same way, since `ChatRequest` allows 0–2 for OpenAI's sake. `capabilities()` answers a conservative `ANTHROPIC_UNKNOWN` with **no price** for a model newer than the pinned table, so a current model stays usable while a typo still fails as the API's own 404 — `provider.py`'s docstring was amended to state both spellings and the rule that binds them: never guess a price. One capability `P1-02` lacks: an image **in a tool result** survives here, where Chat Completions forced a refusal. Security: the key is an injected `KeyLookup` read once per request and never stored (asserted over `vars(provider)`), it goes in `x-api-key` and a test asserts **no** bearer header leaks alongside it, and no error carries a response body — including the **mid-stream `error` event**, which is this API's way of failing *after* a 200 and which a status map alone would never see (`overloaded_error` → transient, everything else → protocol, prose never quoted). 61 new tests (360 pytest, +61; desktop/renderer/scripts untouched); ruff, `mypy --strict`, eslint, tsc, prettier clean and `gen:types:check` still passes — these shapes are internal, so no TS moved. **Exercised by hand against a real loopback server** (not the mock transport): validate_key 200, system prompt lifted, three turns coalesced to one `user`, streamed text → assembled tool call `{"path": "C:\\Users"}` → usage priced at 1.05 cents for 1000 in + 500 out → `tool_calls`; `x-api-key` seen by the server, no `authorization` header, key read once per call and absent from the instance; and a stream **abandoned mid-flight closed silently** under `-X dev`, which is the `P1-02` preemption gotcha staying fixed through the shared reader. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 0.83 s, `/v1/health` 200, gone on terminate. The Electron app was **not** relaunched: no MAIN, renderer, preload or core runtime code was touched and nothing imports the adapter yet. **Honest gap: no live call to Anthropic itself** (no key on this machine), so the wire format is proved against the recorded shape and a local server, not the vendor — `AEGIS_ANTHROPIC_TEST_KEY` runs the live smoke test, and `P1-10`'s Test button is the real proof. The **price table is a dated snapshot and lists only the 4.x line and two older aliases**; a newer Claude works but reports no cost until someone adds it. Gotcha: a `process_iter` orphan check matching `'aegis_core' in cmdline` also matches the checking script itself — match the executable, not the command line                                                                                                                                                                                                                                                                                                                                                                         | P1-04`google` adapter                                                                                                                                                                      |
| 2026-09-17 | claude-code | P1-04                              | The`google` adapter — the third and last wire format; from here on P1 is configuration and wiring. `GoogleProvider` speaks `:streamGenerateContent?alt=sse`, reusing `common.py` for the SSE reader, status map, cost sum and client, so the only new code is Gemini's own vocabulary — and it disagrees with the other two in more places than they disagree with each other. Four differences are load-bearing (Decision Log): the assistant is called **`model`** and there are only two roles; a tool result is addressed **by name**, since Gemini has no id anywhere, so `_call_names` walks the conversation to resolve a `tool_call_id` and the ids in a Gemini `ToolCall` are **minted by this adapter** — a result for a call that is not in the conversation is refused rather than sent under a guessed name, which would answer the wrong call; **tool calls arrive whole** (`args` is a JSON object in one part, not a fragmented string) so there is nothing to buffer and no half-parsed call to guard; and there is **no tool-call finish reason** — a turn that asked for a tool ends `STOP` like any other — so `resolve_finish_reason()` decides from what came back, with `length`/`content_filter` still winning because they say the turn was cut short and the calls in it may be incomplete. **A real bug the tests caught:** `flush_tool_calls()` empties the record the finish reason is derived from, so resolving it *after* the flush silently reported `stop` for every tool turn; it is now resolved before, with a comment saying why the order matters. A `thought` part (2.5-series reasoning, flagged `thought: true`) is **dropped** — showing it would put the model's scratchpad in the timeline as if it were the result — while `thoughtsTokenCount` **is** added to output tokens, because the user is billed for it and under-reporting is the direction that overspends. A prompt blocked before generation finishes `content_filter`: a refusal is an outcome, not an error. Security: the key goes in the `x-goog-api-key` **header, never the `?key=` query the docs lead with**, because a URL carries into access logs, proxy logs and crash reports — a test asserts the key appears nowhere in the URL; the model id in the path is percent-escaped because it comes from settings the user typed, and a test feeds it `../../models/other:generateContent?x=y` and checks `raw_path`. `validate_key` treats a **400** as a rejected key, because this API answers a bad key with 400 and telling which 400 it is would mean reading a body that may quote the key back; in `chat` a 400 stays a protocol error, which is non-retryable either way, so the router never shops for a yes. 65 new tests (425 pytest, +65; desktop/renderer/scripts untouched); ruff, `mypy --strict`, eslint, tsc, prettier clean, `gen:types:check` still passes. **Exercised by hand against a real loopback server**: validate_key 200, system prompt lifted to `systemInstruction`, roles `user`/`model`/`user` with the tool result and the follow-up coalesced, the result addressed by the resolved name `fs.list_dir`, the **thought part absent from the answer** (`"Looking."`), thinking tokens billed (output 500 = 400 + 100) at 0.155 cents, finish `tool_calls` despite the provider saying `STOP`, `x-goog-api-key` on the wire with the key **nowhere in the URL**, and a stream abandoned mid-flight closing silently under `-X dev`. Then the **real core** started the way MAIN starts it: handshake 0.83 s, `/v1/health` 200, gone on terminate, 0 orphans. The Electron app was **not** relaunched: no MAIN, renderer, preload or core runtime code was touched and nothing imports the adapter yet. **Honest gaps:** no live call to Google itself (no key here) — `AEGIS_GOOGLE_TEST_KEY` runs the live smoke test; and the price table uses the **under-200k** rates, while Gemini charges more above that, so a very long prompt is under-reported — `Capabilities` has one price per direction, and whether tiered pricing is worth a shape change is a `P1-09` decision. Gotcha: httpx's `url.path` is the **decoded** view, so an escaping test that reads it sees `../..` and fails; assert on `raw_path` (and strip the query, which `raw_path` includes)                                                                                                                                                                                                                                                                                                 | P1-05`nvidia`/`openrouter`/`custom` as base-URL configs over `openai` — configs only, do not fork the client                                                                        |
| 2026-09-18 | claude-code | P1-05                              | The compatible configs — the point in P1 where a provider costs a constant, not a module.`models/providers/compatible.py` holds `NVIDIA`, `OPENROUTER` and `custom_config(base_url)`, and nothing in it opens a socket: every call goes through `P1-02`'s `OpenAIProvider`, which was not touched except to make one hard-coded path a config field. It sits beside `openai.py` rather than inside it because that module's docstring promises it names no other provider — the moment a second one is named there, a subclass is the obvious next step, and `REMEMBER.md § 4` says not to (Decision Log). **Three judgement calls, each tested.** (1) `openrouter` and `custom` cannot enumerate their catalogues, so an unknown model gets `COMPATIBLE_UNKNOWN`: permissive on vision/tools/JSON — the `ANTHROPIC_UNKNOWN` argument, refusing a model the user's key pays for is worse than the provider's own 404 — but **conservative on `ctx_window`**, because that one is a budget `P4-04` fills and guessing high costs a 400 halfway through a task, not an error at the start. Price stays `None`: never free. (2) A `custom` base URL is the only part of a provider the user types and it decides where their key is sent, so `normalise_base_url()` checks it: `https` anywhere, **`http` only to loopback** (vLLM and LM Studio are local and plaintext, and must keep working; plaintext to another machine is a key on the wire), no `user:pass@`, no query, no fragment, length-capped, and the refusal never quotes the address back — a test feeds it `?key=sk-…` and asserts the secret is not in the message. (3) **The Test button would have lied on OpenRouter**: its `/models` is public, so `validate_key` returned 200 for any string. `ProviderConfig.key_check_path` is `/key` there, which is the whole fix and is configuration, exactly like the base URL. 42 new tests (467 pytest, +42; desktop/renderer/scripts untouched); ruff, `mypy --strict`, eslint, tsc, prettier clean and `gen:types:check` still passes — these shapes are internal, so no TS moved. **Exercised by hand against a real loopback server** (not the mock transport): a `custom` config entered as ` http://127.0.0.1:PORT/v1/ ` normalised to `http://127.0.0.1:PORT/v1`, streamed `'on it'`, reported 1000 in / 500 out with **`cost_cents` `None`** rather than 0, finished `stop`, and the server saw `POST /v1/chat/completions` + `GET /v1/models` both bearing the key — with no `generator didn't stop` noise under `-X dev`. The three refusals printed their user-facing messages. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 2.35 s, `/v1/health` 200, **0 extra bytes** on stdout, gone on terminate, 0 orphans. The Electron app was **not** relaunched: no MAIN, renderer, preload or core startup code was touched and nothing imports these configs yet. `ARCHITECTURE.md § 5.1` said `nvidia` was a *subclass* of `openai`; it now says a `ProviderConfig`, and names the registry. **Honest gaps:** no live call to NVIDIA or OpenRouter (no keys here), so both base URLs are the published ones and the wire format is `P1-02`'s, proved against a local server — `P1-10`'s Test button is the real proof; and **neither has a model table at all**, so every model on them reports no price until someone adds one, which `P1-09` must treat as unknown and never as zero.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P1-06`ollama` adapter + auto-detect on `localhost:11434` — the privacy story; `normalise_base_url` is there to reuse                                                                  |
| 2026-09-19 | claude-code | P1-06                              | The local provider, and the end of P1's adapter work.**Inherited as `WIP`** from an uncommitted 2026-09-18 session — `RECOVERY.md § 6.2` says assume it is incomplete and possibly wrong, so it was verified rather than trusted, and that found two real defects. `OllamaProvider` **composes** `OpenAIProvider` rather than subclassing or configuring it: `chat()` returns the inner generator directly, so the stop path stays one link long and every byte on the wire is `P1-02`'s proved code. It is a module and not a sixth `ProviderConfig` because a local server differs in two ways a base URL cannot express — it takes **no key** (`requires_key`, the one field added to `ProviderConfig`; `_headers()`'s refusal to send a keyless request is right everywhere a key is what pays, and would have made the offline story unreachable here), and its model table is whatever the user pulled, which `refresh()` asks the server for. A local model is priced **`0.0`, not `None`**: `provider.py` forbids guessing a price, and this is not a guess — so `P1-09` will never pause a task that is genuinely free, and `None` keeps its one meaning. **Defect 1, found by reading `refresh()`:** it asked `/api/show` once per model **serially**, so `detect()` — which runs at startup on a machine that may not have Ollama at all — could be held for `MAX_MODELS` × `DETECT_TIMEOUT_S` by a server that accepts and then stalls, against the module's own comment that discovery may not cost a visible pause. Measured on a real loopback server: **4.44 s for 10 models at 0.4 s each**, now **1.11 s**, with describes bounded twice (8 at a time, one 10 s budget for the set) and a model that times out keeping `OLLAMA_UNKNOWN` rather than vanishing from the list. The regression test itself took **40 s** with the budget removed and 0.1 s with it. **Defect 2:** `detect()` documented that it never raises, but only `default_base_url()` was inside the guard — so `detect(base_url=…)` raised `ValueError`, and that is precisely the call `P1-10`'s Models screen will make with a URL the user typed. Both fixes are **mutation-checked**: reverting to serial describes, removing the budget, and moving the construction back outside the guard each turn the new tests red. 45 tests in the file (509 pytest, +42 from the inherited file and +3 mine; 170 desktop, 142 renderer, 11 scripts, all green); ruff, `mypy --strict` (67 files), eslint, tsc, prettier clean and `gen:types:check` passes — these shapes are internal, so no TS moved. **Exercised by hand against a real Ollama-shaped server on loopback** (not the mock transport): `detect()` returned a provider in 601 ms having discovered three models with their **real** per-model capabilities (`llava:7b` vision-only, `llama3.1:8b` tools-only), `validate_key()` answered *running with 3 models installed*, a streamed call returned `'on it'` with 1000/500 tokens at **`cost_cents` 0.0**, **no `authorization` header appeared on any of the seven requests** the server logged, and a stream abandoned mid-flight closed silently under `-X dev` — the `P1-02` preemption gotcha staying fixed through a second composition layer. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 0.94 s, `/v1/health` 200, **0 extra bytes** on stdout, clean terminate, 0 orphans. The Electron app was **not** relaunched: no MAIN, renderer, preload or core startup code was touched, and nothing imports this adapter yet — `P1-08` is the first caller. **Honest gaps:** no Ollama is installed on this machine, so the live smoke test (`AEGIS_OLLAMA_TEST_MODEL`) is still skipped and the wire format is proved against a hand-built server, not the real daemon — the first machine with Ollama on it is the real proof; and `DEFAULT_CTX_WINDOW` deliberately under-serves a user who raised `OLLAMA_CONTEXT_LENGTH`, which `P1-10` should let them say. **Note for the next session:** a commit `ffb0f28 "next task completerd"` landed on `origin/main` mid-session from outside this session, containing the inherited WIP. It is already pushed, so it was left alone rather than rewritten — but its message does not follow the `P1-06: …` convention `REMEMBER.md § 7` requires, so `git log` no longer maps cleanly to the tracker at that commit. Also: `ruff format --check` flags `actuation/input.py` and `tests/actuation/test_hooks.py`, both pre-existing and untouched here (the P0-10 gotcha) | P1-07 key vault on DPAPI via`keyring` — **security-gated**, so `REVIEW.md § 5` in full; it is also what makes `P1-10`'s Test button real for the six providers that need a key |
| 2026-09-19 | claude-code | P1-07                              | The key vault — security-gated, so most of the work is what it refuses to do.`storage/vault.py` holds one key per provider under the single Credential Manager target `Aegis`. **The decision that matters: it instantiates `WinVaultKeyring` directly and never touches `keyring`'s module-level API** (Decision Log). Invariant 9 says *DPAPI*, not *keyring* — and `keyring.get_password()` picks its backend from a `keyringrc.cfg` and every backend entry point on `sys.path`, so a config file or a third-party plugin could have sent every key to a plaintext file with no test failing and the code still reading "stored via keyring". A test redirects the global keyring and asserts the vault ignores it; another writes and reads through the machine's **real** Credential Manager. `get_key()` reads through to the OS every call and `lookup()` returns a `KeyLookup` callable, so the seam every adapter since `P1-02` was built against is now real — an adapter gets the means to fetch a key, never a key. Three input rules, each tested: surrounding whitespace is stripped (a pasted key carries a newline, and the provider would reject it for no visible reason), length is capped at 1024 (Credential Manager's own 2560-byte blob limit, as a message a person can act on), and **any control character is refused** — a key goes straight into an HTTP header, so a CR or LF in one is header injection. `mask_key()` is the only display form: first 3 + last 4, and **nothing at all below 16 characters**, because seven characters of a fifteen-character key is most of it; a property test walks every length 1–79 and asserts the revealed count is zero or under half. A `VaultError` carries the backend exception's **type**, never its message, since the value that backend was handed is the key. 38 new tests (547 pytest, +39 — the flaky hook test also passed this run; 170 desktop, 142 renderer, 11 scripts, all green); ruff, `mypy --strict` (69 files), eslint, tsc, prettier clean and `gen:types:check` passes — no route, no bridge member, no renderer path, so no TS moved. **Five mutations, all caught red:** dropping the control-character check, letting the backend's own message into the error, adding the key to the save log line, resolving the backend through `keyring`'s global config, and widening the mask to 8 characters. **Exercised by hand against real DPAPI across two processes**: saved in one process and read back in a fresh one (the P1 gate's *keys survive an app restart*), masked as `sk-…WXYZ`, a real `LegacyGeneric:target=Aegis` entry in `cmdkey /list` with the key **not** in its output, the key absent from argv, from the environment, from every `core.log`, and **not present in plaintext anywhere in `%APPDATA%\Microsoft\Credentials`** — then removed, leaving nothing behind. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 0.70 s, `/v1/health` 200, **0 extra bytes** on stdout. And because a new *runtime* dependency (`keyring` 25.7, pulling `pywin32-ctypes` — pure ctypes, no compiled extension, which `P7-01` will be glad of) changes how the core is spawned, the **app itself was launched**: core up, `/v1/health` 200, `stream.connected`, Electron force-killed → **0 orphan cores**. **Honest gaps:** nothing imports the vault yet — `P1-08` is the first caller and `P1-10` the first screen, so *never the renderer* is currently true by construction rather than by a test, and it needs re-checking the moment a route exposes a key status; and the one key-per-provider shape means a user cannot keep two `custom` gateways, which `P1-10` should confirm is acceptable. Gotcha: `proc.stdout.read(1)` after the handshake line **hangs** — the core repoints `sys.stdout` at a sink but the pipe never reaches EOF, so terminate the core first and read what is left.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P1-08 router: role map (Planner/Grounder/Utility), fallback chain, capability gate — the first thing to import both the adapters and the vault                                              |
| 2026-09-20 | claude-code | P1-08                              | The router — the first module to import every adapter**and** the vault, and the point where the model layer stops being a pile of clients. `models/router.py` holds `RoleMap` (`ARCHITECTURE.md § 5.2`'s three roles, each a `primary` plus up to three `fallbacks`, bounded and refusing a chain that names the same model twice), `Requirement` + `ensure_capable` (the § 5.3 gate), `ModelRouter.chat` (the chain) and `ProviderPool` (one adapter per provider id, since each owns a connection pool). **Four judgement calls, each a Decision Log row and each mutation-checked red.** (1) The chain is walked **only before the first delta reaches the consumer**: § 5.3's "429/5xx/timeout" says when an error is retryable and nothing about a stream that has already produced output, and restarting after text is in the timeline repeats it and re-issues a tool call the Guardian has already seen. (2) The gate checks the **primary only** — a blind spare must not brick a working primary, and must not silently answer a vision question either, so an incapable fallback is dropped from the chain with a log. (3) `RoleRequest` has **no `model` field**, because each attempt genuinely runs a different model and a request carrying one would be wrong for every attempt but the first; `bind(choice)` is the only place a model is named, and merges the user's saved params under whatever the request itself asks for. (4) The router **closes the adapter's generator itself** in a `finally`: `async for` does not, so the consumer abandoning the router's stream (preemption) would leave the adapter suspended on a dying socket — the `P1-02` gotcha, one layer up. Security: the pool holds a `KeySource` and never a key (asserted over `vars`), a saved `custom` address is re-run through `P1-05`'s `normalise_base_url` on every build because settings can be old or hand-edited, and `ProviderUnavailableError` is a `RouterError` rather than a `ProviderError` so a missing gateway address is a settings problem the user fixes, not a hiccup to retry around. 59 new tests (606 pytest, +59; 170 desktop, 142 renderer, 11 scripts, all green); ruff, `mypy --strict` (71 files), eslint, tsc, prettier clean and `gen:types:check` passes — these shapes are internal, so no TS moved. **Exercised by hand against a real loopback server** (not the mock transport): a `custom` primary answering 429 fell over to the second model on the chain and streamed `'on it'` with 1000/500 tokens at `cost_cents` **`None`** (never 0 for an unknown price), the server saw both requests in order with `Bearer` on each, a blind grounder was refused **before any request reached the wire** (*llama3.1:8b is mapped to grounder but has no vision*), and a stream abandoned mid-flight closed silently under `-X dev`. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`): handshake 0.80 s, `/v1/health` 200, **0 extra bytes** on stdout. The Electron app was **not** relaunched: no MAIN, renderer, preload or core startup code was touched, no dependency was added, and nothing imports the router yet. **Honest gaps:** nothing calls it — `P1-09` is the first caller and `P1-10` the first screen — so the fallback chain is proved against a server that fails on demand rather than against a real provider outage; and the `P0` gate note now points at `P1-10` rather than here, because a router with no route, no bridge member and no saved key still cannot make the composer round-trip real.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P1-09 budget guard: per-task + per-day ceilings, live cost events — pause on breach, never continue                                                                                         |
| 2026-09-20 | claude-code | P1-09                              | The budget guard, and with it the core's first durable writer.`models/budget.py` decides *whether to ask at all*; `storage/usage.py` remembers what was asked. The spend lives in a **new ninth table** (`usage`, `m0002_usage.py`, `ARCHITECTURE.md § 7` updated) rather than in `tasks.cost_cents`, because a per-day ceiling that an app restart clears is not a ceiling — and `tasks` has nowhere to put tokens, no way to tell an unknown price from zero, and no row at all for a call made outside a task. **Four judgement calls, each a Decision Log row and each mutation-checked red.** (1) A breach is refused **at the door**: every adapter emits its one `UsageDelta` at the *end* of a stream, so by the time a cost is known it is spent — aborting there throws away an answer the user paid for and leaves half a turn in the timeline, while `check()` stopping the *next* call is exactly where § 5.3's "pause the task and ask" belongs. `record()` still publishes `cost.updated` with `breach` set, so the UI pauses immediately. (2) **Token ceilings as well as cents ceilings**, because an unknown price is never summed as zero and a cents-only guard therefore never fires at all on a `custom` gateway or an unlisted OpenRouter model — the providers a privacy-minded user picks. The backstop (10M/task, 100M/day) sits far above any honest task, so a priced provider always meets its cents ceiling first. (3) The day is the **local** date, stored as its own column: "ten dollars a day" is a promise about the user's day, and a stored boundary cannot be re-derived differently later. (4) The ledger is the core's **first long-lived connection** — one, `cross_thread`, under a lock — which is the question `P0-10` deliberately left to its first real writer. `ModelRouter.chat` gained an optional `guard` and a `task_id` and is unchanged without one, so `P1-08`'s 59 tests passed through untouched. **One defect found by hand:** below ten cents, money rendered two decimals, so a spend of 0.75¢ against a 0.5¢ limit both read `$0.01` — the message said a number had reached itself; small amounts now get four decimals, with a regression test. 46 new tests (652 pytest, +46; 170 desktop, 142 renderer, 11 scripts, all green); ruff, `mypy --strict` (76 files), eslint, tsc, prettier clean and `gen:types:check` passes — these shapes are internal, so no TS moved. **Exercised by hand against a real loopback server** (not the mock transport), on a real SQLite file: a **priced** `openai`-config model streamed, was billed **0.75 cents** from its own price table, tripped `task_cents` and the next call was **refused before the wire** (the server saw no second request); an **unpriced** `custom` gateway ran with its cents total stuck at 0.0 and `unpriced_calls` climbing until `task_tokens` stopped it; and the totals were still there after closing and reopening the ledger. No `generator didn't stop` noise under `-X dev`. Then the **real core** started the way MAIN starts it (token on stdin, `--supervisor-pid`) on a deliberately **v1 database**: it migrated in place to v2, kept its existing `tasks` row, logged `storage.migrated`, answered `/v1/health` 200 and wrote **0 extra bytes** to stdout — every existing install takes that path, so it has a test too. The **Electron app was launched**, because a migration now runs on every startup: core up on the migrated v2 database with the `usage` table present, and 0 orphan cores once MAIN was gone. **Honest gaps:** nothing persists user-chosen limits yet — the defaults ($1.00/task, $10.00/day) are constructor arguments until `P1-10` writes them to `settings`, so a user cannot change a ceiling; and no route, bridge member or screen publishes `cost.updated` to the renderer, so the payload is proved against a fake hub in the guard's own shape rather than end to end. Gotcha: `Stop-Process` on what `Start-Process` returns for `electron.cmd` kills the **shim**, not Electron — the cores looked like orphans until the real PID was checked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P1-10 Models screen: role cards, provider cards, Test button, spend meter — it is the first task to put a model behind the composer, so it closes**both** P1 and the P0 gate          |
| 2026-09-20 | claude-code | P1-10                              | The Models screen, and with it the first route that writes. Two new core modules:`storage/settings.py` (the `settings` table — bounded values, and a document this build cannot read is *absent*, because the only screen that could replace a bad one is the screen that would otherwise fail to open) and `models/service.py`, the one owner of the settings document, the vault and the ledger, and the only place the wire shapes meet `RoleMap`/`BudgetLimits`. **Seven judgement calls, each a Decision Log row.** The three that matter most: keys get **two routes and no reader** — `PUT`/`DELETE /models/keys/{provider_id}` is the only way one enters, `has_key` and `sk-…abcd` are the whole vocabulary coming back, and a test saves a key then searches every other response for it; `GET /models/catalog` **calls no vendor**, so the screen you open when nothing works opens with no internet, and the three providers that cannot be enumerated say so and take a typed model id; and a malformed request is now a **422 that repeats none of it**, app-wide, because FastAPI's default echoes the input and the renderer is both a hostile caller and the field a key is typed into. Renderer: `ModelsScreen` plus `ModelRoleCard` / `ProviderCard` / `SpendMeter` from the `UI.md § 12` inventory, a saved/draft split so *Save changes* and *Discard* mean something, and a spend bar that reads the last `cost.updated` off the stream rather than polling. **Driven in the built app over CDP** (not dev mode): all three roles mapped, a $3.50 daily ceiling set and saved, a key written to the real Credential Manager and read back masked, Ollama tested live in 11 ms, the app restarted with everything still there, then the key and the settings row removed again. That run caught one real defect — `edit(value)` closed over a stale draft, so two edits in one React batch lost one; `edit` now takes an updater and a regression test does the same thing in one `act`. Green: 720 pytest, 274 renderer, 170 desktop, 4 scripts; ruff, mypy --strict, eslint, tsc, prettier all clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P2-01 per-monitor DPI                                                                                                                                                                        |
| 2026-09-21 | claude-code | P2-01                              | The coordinate model every click will depend on. Probed first: this machine is one 3200×2000 panel at 200 %, and a default `python.exe` is **DPI-unaware** — it is told 1600×1000 while `SendInput` spans the real panel, so without this every click lands at half its coordinates, silently. New `perception/display.py` + `perception/win32.py`: one space (physical virtual-desktop pixels), Per-Monitor V2 set first thing in `main()`, `query_layout()` refusing an unaware thread, a validated `DisplayLayout` snapshot with dead-zone-aware `monitor_at`, `to_absolute` for `SendInput`, a fingerprint, and `verify_layout` for display changes. Five Decision Log rows. The strongest test is a live round-trip — ~230 points out through real `SendInput`, back through `GetCursorPos`, every one exact; with the usual `65535/(w−1)` formula mutated in, it misses. Built app exercised: its core logs `display.dpi_aware PER_MONITOR`, and killing MAIN left no orphan. Green: 794 pytest, 274 renderer, 170 desktop; ruff, mypy --strict clean. **Not verified on real multi-monitor or 125/150 % hardware** — only synthetic layouts. Pre-existing `ruff format --check` drift in `actuation/input.py`, `tests/actuation/test_hooks.py`, `tests/models/test_service.py` left alone (not this task; `py:lint` does not check format). | P2-02 `screen.capture()` via `mss` — note `mss` calls `SetProcessDpiAwareness` itself on init; ours runs first, so its call is refused and harmless, but check it. Or finish P3-01's absolute `move_to` on top of `DisplayLayout.to_absolute` |
| 2026-09-21 | claude-code | P2-02                              | Screen capture. New `perception/screen.py`: `capture()` for a monitor, a window or a region → a full-resolution `Frame` stamped with the layout it was measured under; `Frame.encode()` → a ≤1280 px WebP `Screenshot`. Read `mss`'s source first, as P2-01's handoff asked: it is GDI `BitBlt` (not DXGI, as the stack table claimed — corrected) and calls `SetProcessDpiAwareness` every time one is constructed, so the DPI check now always runs before `mss` exists, and a test proves an unaware thread is refused without `mss` being built. The budget was the real work: full-res WebP alone is ~700 ms, the first working draft ran 130–160 ms and failed its own budget test 2 runs in 6; a shared `mss` object (−20 ms), box resampling on a zero-copy BGRX view (−14 ms, channels swapped on the small image) and WebP method 0 brought it to **median 115 ms, worst 143 ms**, checked by eye for text legibility on a real capture. Four Decision Log rows. Tk was tried for the live window tests and dropped — repeated `Tk()` roots intermittently failed to load `init.tcl` — for a 150-line ctypes `SolidWindow` in `tests/perception/helpers.py`. Mutation-checked: channel swap, post-grab verify, DPI-before-`mss`, minimised refusal, dropping a broken `mss`, pixel cap — each turns a test red. Built app run: core logs `display.dpi_aware PER_MONITOR`, stream connects, killing Electron leaves no core. Green: 856 pytest, 274 renderer, 170 desktop; ruff, format, mypy --strict clean. Gotchas: a shell spawned from VS Code carries **`ELECTRON_RUN_AS_NODE=1`**, which makes `pnpm --filter @aegis/desktop start` run Electron as plain Node and die on an ESM error — clear it first. P2-01's live `SendInput` round-trip failed once in ~10 runs this session with one point off by ~100 px — consistent with the real mouse moving mid-test; left alone. **Not verified:** multi-monitor, 125/150 % and a locked machine on real hardware (synthetic layouts and a faked `BitBlt` refusal only). | P2-03 `uia_tree.walk()`. Remember **P2-05 redaction is not in yet** — nothing may send a `Screenshot` to a model before it lands. P3-13 now carries a note about `WDA_EXCLUDEFROMCAPTURE` |
| 2026-09-22 | claude-code | P2-03 | **Inherited WIP**: `uia_tree.py` had been written in an unlogged 2026-09-21 session and then committed by hand as `1c45d91 "next task completed"`, with no tests. Verified per `RECOVERY.md § 6.2` rather than trusted, and it was close but not done: `mypy --strict` failed (comtypes has no stubs), `comtypes` was not a declared dependency (installed by hand — `pip show` lists no dependant), and **every element without a value pattern reported `value == ""`** because a cached property reads as its default when unsupported, so a button and an empty text box were indistinguishable — found by walking Notepad and VS Code, fixed via `IsValuePatternAvailable`. Added a lock around type-library generation. New `FormWindow` in `tests/perception/helpers.py` — a real Win32 window with button / disabled button / edit / `ES_PASSWORD` edit on its own pumping thread, able to `hang()` — which `P2-05`'s golden redaction test can reuse. 66 tests; 9 mutations all red, two only after a fake-COM test was added, because Windows' default connection timeout happened to be 2 s too, so the live hang test could not see our setting. Built app launched: core up, stream connected, killing MAIN left no core. Green: 921 pytest + renderer 274 + desktop/scripts; ruff, format, `mypy --strict` (90 files) clean. **One failing test, not this task's:** `test_capture_and_encode_fit_the_budget` (P2-02) ran at a ~180 ms median all session — it fails with `test_uia_tree` not even collected, and splits as capture 78 ms + encode 97 ms against P2-02's ~53 + ~60, on a machine at ~28 % CPU with a full-screen Notion page in front (encode cost depends on content). `screen.py` was not touched. Decide whether the budget test should pin its content, or whether 150 ms is only reachable on an idle machine. Two Decision Log rows. **Not verified:** multi-monitor and 125/150 % (the bbox path shares P2-01's model, tested at 200 % only), and a real provider that connects then stalls mid-transaction (tested with a fake). | P2-04 pruning + relevance ranking to 200. Note Electron apps without accessibility (P2-07 note). Or P2-05 redaction — `FormWindow` already has the password box its golden test needs. Look at the P2-02 budget failure first |
| 2026-09-22 | claude-code | P2-04 | Pruning + ranking. Measured three real windows before writing rules — Explorer (243 elements), an editor (808) and Notepad (54) — and the rules come from what they showed: offscreen alone is ~40 % of the editor, but Explorer's noise is **redundancy** (four cell `Edit`s per file row, one repeating the row; an unnamed `Image` in every toolbar button; breadcrumb `Text` repeating its `SplitButton`), and the editor wraps buttons in same-named `Group`s. New pure `perception/prune.py` rather than more code in the COM module; candidates keep the walk `id` so `P2-06`/`P2-08` index the same tree, come back in document order, and carry the visible rectangle. Two defects found by running it live, both fixed with tests: **Notepad reports three unnamed panes as focused at once**, so "pin the focused element" pinned all three — now one, the innermost meaningful claimant; and long prose lines outranked the *Run* menu for "run the tests" — goal words now count only in a ≤ 12-word label. 3 ms for 808 elements; a 5 000-element tree is asserted under 200 ms. 44 tests, 15 mutations red (the "first focus claimant" mutant survived until a nested-focus test was added). Four Decision Log rows; `ARCHITECTURE.md § 4` + `§ 6.3` updated. Green: 966 pytest, 274 renderer, 170 desktop; ruff, format, `mypy --strict` (92 files) clean. Built app launched, core up and stream connected, killing Electron left no core. **The P2-02 budget test failed once more** in the full run (median ~184 ms, with an Explorer window I had opened on screen) and passed 3/3 alone and in a full re-run after closing it — content- and load-dependent, as the last session said; still undecided. **Not verified:** the weights against real tasks (`P4-12` does not exist), and multi-monitor on real hardware (dead zones are synthetic). | P2-05 redaction (security-gated; `FormWindow` has its password box). Its tree half should run on the `UiaTree` **before** `prune()`, so a secret-looking label never ranks. Decide the P2-02 budget test |
| 2026-09-22 | claude-code | P2-05 | Redaction, security-gated. Measured before designing: this desktop's z-order (only the taskbar above the foreground window, not overlapping it by DWM bounds), and that UIA's root rectangle equals `GetWindowRect` for Notepad, Chrome and the taskbar. That made two rules cheap and safe: **pixels no walked tree vouches for are black** (a monitor capture would otherwise ship a background window's password box, and a topmost popup over the target is in no tree), and a window that **moved** since its walk is refused rather than boxed in the wrong place. New `perception/redact.py`; `Frame.encode`/`to_image` made private; `UiaElement`/`UiaTree` gain a `redacted` flag and `prune()` now refuses an unredacted tree. "The test that fails if the call is removed" is an **AST scan** of the core, since there is no caller to remove yet; checked against a planted `frame._encode()` in `prune.py`. Golden test: a real `ES_PASSWORD` box captured off the screen is exactly black in the frame bytes and ≤ 8 in the WebP; a real topmost window over it is black. 78 tests, 17 mutations red. One false failure in my own test fixed: a "text box untouched" sample point landed on a black glyph. Four Decision Log rows; `ARCHITECTURE.md § 4` + `§ 8.4` updated. **Privacy note:** one manual full-monitor check saved a PNG of the user's screen to the session scratchpad; it was viewed once and deleted, and all later verification used test windows only. Green: 1045 pytest, 274 renderer, 170 desktop; ruff, format, `mypy --strict` (94 files) clean. Built app launched, core up, no orphan after killing Electron. **Two failures not this task's:** P2-02's budget test failed every run (~205 ms median) and fails identically on the stashed baseline, with a photo-heavy page in the foreground — the content-dependent flake again, and it now needs a decision; P2-03's live foreground-window test failed once with UIA `0x80004005` on a Chrome window and passed on re-run with and without these changes. **Not verified:** multi-monitor and 125/150 % on real hardware; text with no tree behind it (`P2-07`). | P2-06 set-of-mark: draw marks on `Redacted.frame`, never a raw `Frame`, and extend the AST scan's allow-list rather than bypass it. Decide the P2-02 budget test first |
| 2026-09-23 | claude-code | P2-06 | Set-of-mark — the last piece of perception the model actually looks at. New `perception/mark.py`, not the drawing step inside `screen.py` that `ARCHITECTURE.md § 6.3` described: marking needs a `Redacted` and a `PrunedTree`, and `redact.py` already imports `screen.py`, so putting it there is an import cycle and, worse, puts the drawing in the one module that has never known what redaction is. **Three choices carry the task.** (1) `mark()` takes a `Redacted`, never a `Frame` — the type is what makes 'a mark can only sit on pixels already cleared to leave' true rather than a rule someone remembers. (2) The chip carries the candidate's **walk id**, so the number the model reads and the id `P2-08` resolves are one thing; a 1..N renumbering would be a second mapping to keep in step, which is exactly what P2-04 kept the walk id to avoid. (3) The marks go on **after** the downscale — a 2 px outline and a 13 px digit drawn at 3200 px and then box-filtered reach the model as grey smudges — which needed `Frame._encode` split into `_to_image` + a new private `screen._encode_image`. That split widens the redaction allow-list, so it was **extended, never bypassed**: both names are in `test_redact.py`'s source scan with `mark.py` named, and a parametrised case feeds the scan both spellings of the new call to prove it still bites. Security beyond that: **only digits are ever drawn**, asserted by recording every string that reaches Pillow; a real redacted password box is still black after marking; marking changes no pixel far from the element it marks; the log carries counts, never a name; and the input frame is never mutated. An id with no box on the picture is the other failure worth naming — a model cannot aim a click it was not shown — so a candidate off the region or under `MIN_BOX_PX` at output scale comes back in `Marked.unmarked` rather than being dropped in silence, and `Marked.ids` is the exact set an answer may name. **Verified by eye, not just by assert**: a real window walked → captured → redacted → pruned → marked, the WebP decoded and looked at — five boxes in cycling colours, numbers legible at 4× zoom, the password field solid black under its own mark. Measured: ~60 ms to draw 200 marks on 1280×800, against ~100 ms for the encode of the same image; the budget test therefore asserts the **drawing**, because an end-to-end assertion would be a second copy of P2-02's budget test and that one is content-dependent. 26 new tests (1072 pytest); ruff, format and `mypy --strict` (96 files) clean. Real core started the way MAIN starts it: handshake 1.04 s, `/v1/health` 200, **0 extra bytes** on stdout, gone on terminate, no orphan. The Electron app was **not** relaunched — no MAIN, renderer, preload or core startup code was touched, and nothing imports `mark.py` yet. **Two failures that are not this task's, both reproduced on the stashed baseline and now tracked**: P2-02's budget test (`P2-10`) and P2-05's golden-image test, whose *precondition* samples a black password dot at 192 DPI rather than background (`P2-11`) — redaction itself is fine and was re-checked by eye. **Not verified:** multi-monitor, 125/150 % scaling, and how the marks read to an actual model, which is `P4-03`'s prompt work. | P2-08 grounding: `resolve(element_id)` takes the same walk id `mark()` draws, and `Marked.ids` is the allow-list of ids an answer may name |
| 2026-09-24 | claude-code | P2-08 | Grounding: the model's element id turned into a click point, verified against the screen **as it is now**, not as the screenshot showed it. New `perception/grounding.py`, plus `uia_tree.hit_chain()` and `win32.root_window_at()`. **Measured before designing**: runtime ids were identical across two walks of every element of a 1 769-element Electron window, so they are the identity; and a UIA hit test at candidates' centres met the target or something inside it 55/60 times — the misses were an *ancestor* (coarse providers: a toolbar answers for its buttons) or a *different* element. That split is the rule: target / inside / bare ancestor pass, anything else is something drawn over the element and refuses; Windows' own `WindowFromPoint` runs first and names the other-window case. A changed name refuses (*Play* → *Pause*), compared after running the fresh tree through the same `redact_tree()`. On a real **Explorer** window (the phase gate's target, opened on an empty scratchpad folder, nothing clicked, closed afterwards) 69/83 candidates ground, 8 disabled toolbar commands refuse correctly, and the covered-refusals caught a real hazard: the centre of the address box is a breadcrumb **button**. The 3 false refusals are title-bar min/max/close — UIA gives them a different runtime id through its MSAA proxy — left refusing on purpose (Decision Log; the `window` tools own them). A targeted `FindFirst` was measured and deferred. One test assumption was wrong and the code was right: a plain `STATIC` over a button is click-through, so it does not cover it — now pinned by a test, with an `SS_NOTIFY` label as the real cover. 58 new tests; ruff, `mypy --strict` (98 files) clean; 1130 pytest pass. **Failures:** P2-11's golden precondition (known, fails on the baseline); at session start P2-10's budget test and **two preemption hook tests** also failed, and all three passed at the end — the hook pair passed 14/14 alone too, and is now `P3-16` because a preemption miss is not a flake to shrug at. `ruff format --check` reports 3 pre-existing unformatted files not touched here (`actuation/input.py`, `tests/actuation/test_hooks.py`, `tests/models/test_service.py`); left alone, and `py:lint` does not run format. The Electron app was **not** relaunched: no MAIN, renderer, preload or core-startup code changed and nothing imports `grounding.py` yet; the core's startup import (`display.py`) is untouched. **Not verified:** multi-monitor, 125/150 %, and runtime-id churn in virtualised lists over a real task. | P2-07 OCR fallback is topmost; or `P3-01`'s absolute `move_to`, which must `verify_layout(grounding.layout)` immediately before `SendInput` |
| 2026-09-24 | claude-code | P2-07 | OCR fallback, security-gated because it changes redaction. The design question was where OCR sits, and the answer is **inside `redact()`**, on the already-painted frame: OCR turns pixels into model-bound text, which is the route invariant 8 guards, so running it anywhere else would either read a password box or another app's window first or need a second redaction path. Inside, `ocr.py` gets bytes from `redact.py` and never a `Frame`, so the source scan needed no new allowance; and a blind region is **read and redacted, or black** — never sent unread. That also closes P2-05's stated gap (text with no tree behind it). **The blind-region rule took three measured iterations**, each fixing a real failure: "nothing inside speaks" flagged Chromium's empty render pane over 98 % of the IDE; counting a large element's name hid the terminal buffer (a `Text` named after its tab); counting any value hid the IDE's code editor and integrated terminal, because Chromium's page `Document` carries its URL as a value. The final rule — large, no value, < 25 % covered by describing elements from anywhere in the tree, a large value counting only when nothing inside it speaks — gets every live window right. Tests caught three of my own mistakes, all fixed: an always-true `assert (a, b, c)`; two tests that "described" a window with one big element the rule deliberately ignores; and a "clean" test line containing the word *secret*, which is itself a secret label. Two scan-ordering holes were closed before any test existed for them, then tested: the line cap and the text cut both now run **after** the secret scan. **Live:** Windows Terminal showing a fake key (assembled at run time) — black in the real pixels, `[redacted]` in text, the rest read, ~100 ms; on a covered IDE window redaction had already blacked the blind regions so OCR read nothing, which is the property, observed. Paint, Terminal and Notepad were opened for measurement and closed; nothing was clicked or typed, and no screen text was printed except in the test windows. **Tesseract split out as `P2-12`**: no binary here to verify against, and running one from `PATH` would hand screenshots to whatever is called `tesseract.exe` there; a machine with no OCR language fails closed today. OCR lines as click targets split out as `P2-13`. 45 new tests; ruff, format and `mypy --strict` (100 files) clean; 1174 pytest pass. **Failures:** P2-10's budget test and P2-11's golden precondition, both known and both unchanged — P2-11 still fails on its line 593, *before* `redact()` is called. The preemption pair (`P3-16`) passed this run. The Electron app was not relaunched: core startup imports only `display.py`, and nothing outside perception imports `redact.py` yet. **P7 note:** PyInstaller must collect the `winrt` `.pyd`s. **Not verified:** OCR at 100/125 % scaling, non-Latin scripts, multi-monitor. | P2-09 perceptual hash is topmost; `P4-04` must pass `Redacted.text` and `.unread` to the planner as untrusted data |
| 2026-09-24 | claude-code | P2-11 | GitHub housekeeping. `main`'s CI had gone red on the P2-05 and P2-08 pushes (runs `35874029682`, `35974755051`) — the P2-11 golden precondition, the same failure every local run here reported. A Copilot agent fixed it in PR #1, which was merged (`9f75b90`, green); P2-07 was then merged on top (`72ab485`) and its CI is **green** too, which also settles the worry that GitHub's Windows runner might lack an OCR language (it would have failed closed and broken the redaction tests). Reviewed the fix: it is exactly what this row prescribed, and it passes 3/3 locally. Marked DONE crediting PR #1. PR #2 duplicates PR #1 with no other change: redundant, to be closed on GitHub — not possible from here (no `gh`, no token). No code changed in this entry. | P2-09 perceptual hash is topmost |
| 2026-09-24 | claude-code | P2-09 | Perceptual hash for the stuck detector. New `perception/phash.py`. **I measured before choosing**, on the real 3200x2000 desktop (hashes in memory only, no image saved) and on a UI-shaped fixture. UI screens are mostly flat colour, and that decided the design. The textbook 64-bit dHash moved only 3 bits for a 500x700 dropdown, so I used a **17x16 grid (256 bits)**, which moves 9 for the dropdown and 33 for a dialog. A strict `>` let ±2 levels of noise flip 4 bits, all of them ties between flat cells that rounded apart, so a bit needs a **1-level dead band**: noise is then 0 at up to ±8. `NEAR_IDENTICAL_BITS` = 5. It cannot see a caret, a typed character or a checkbox, on purpose; a stuck call also needs an unchanged plan (`P4-06`). It hashes a **`Redacted`**, never a `Frame`, because the hash is persisted. So `phash.py` joins `mark.py` on `test_redact.py`'s `_to_image` allow-list (one name, one file, the reason beside it), and a test shows two screens that differ only inside a password box hash the same once redacted, and differently if not. 41 tests; 6 mutations (no band, `>=`, `<` threshold, self-compare, `match` instead of `fullmatch`, allow-list removed) all red. Two Decision Log rows; `ARCHITECTURE.md` § 4, § 6.1, § 6.3 (new item 5) and § 8.4 updated. No app surface or caller yet, so there is nothing in the built app to exercise; I verified it live against the real screen instead. Pre-existing and left alone: `ruff format --check` flags `actuation/input.py`, `tests/actuation/test_hooks.py`, `tests/models/test_service.py`. Green: 1217 pytest (4 key-gated skips), 170 desktop, 274 renderer, 11 scripts. | P2-10 is topmost |
| 2026-09-24 | claude-code | P2-10 | Decided the capture budget test, which only changed a test. **Measured first.** Live screen, idle: grab ~57 ms, downscale ~23, encode ~29. Encode cost depends on content (flat 22 ms, UI-shaped 26, pure noise 70), but **load** is what explains the failures: with every logical CPU busy, grab went to 77 ms and downscale+encode to 127, and the thread's own CPU time doubled too (60 → 116: hyperthreads share cores, turbo drops). So neither fixed content nor CPU time makes a timing test load-proof on a developer's machine. Chosen, from the row's own options: the default suite times **downscale + encode on a fixed, seeded 'busy desktop'** (text, sidebar, a noise 'photo' panel), which is harder than the real screen (236 KiB vs ~97 KiB, 68 ms vs ~55), with a ceiling of the whole 150 ms, since that half is what this code controls. A second test pins that the fixture stays harder than a real screen. The live end-to-end test is **kept unchanged** as a benchmark behind `AEGIS_PERF=1` (the repo's existing opt-in convention), so the skip appears in every run; `P8-04`'s row now says to run it. Mutations: WebP method 4 and a skipped downscale go red; **Lanczos does not** (it costs less than the headroom), noted in the row. One Decision Log row. No app code changed, so there was nothing to exercise in the built app. Green: 1218 pytest (5 opt-in skips: 4 provider keys + `AEGIS_PERF`), 170 desktop, 274 renderer, 11 scripts. | P2-12 is topmost but needs a packaging decision; `P2-13` / `P3-16` / `P3-01` otherwise |
| 2026-09-24 | claude-code | P2-13 | OCR lines as click targets. First, **P2-12 was topmost** but needs a packaging decision; I asked, the owner deferred it, and it is now BLOCKED on that decision. The design question here was how to prove an OCR line is still there when it has no element identity to re-find. Answer: the pixels. `resolve_text()` observes the window again — walk, capture, `redact()` with OCR — and requires the **same text in the same place**. Going through `redact()` means no new route to frame pixels (the redaction scan is unchanged), and a line that now shows a secret is blanked there and cannot match. **Measured first:** six reads of an unchanged self-drawing window gave pixel-identical line boxes, and a re-observation costs ~100 ms, so the tolerance is 2 px per edge. `text_targets()` offers lines by their index in `Redacted.text`: never a redacted line, never a speck, and only a line lying in exactly one walked window's blind region, so grounding knows which window to re-read. `WindowFromPoint` must still land in the window. Not covered, written down: an invisible element in the same window that takes clicks without drawing. The helper `CanvasWindow` gained `set_lines()` so a live test can redraw. 42 tests (3 live: every line grounds inside its drawn band; redrawn text and a scrolled line both refuse). Mutations on text equality, each of the four edge tolerances (the first run showed a left-edge-only mutation surviving, so per-edge cases were added), the window hit test, the unread check, the offer filter, the single-owner rule and the ambiguity check are all red. One Decision Log row; `ARCHITECTURE.md § 6.3` item 4 extended. Nothing calls it yet, so there is nothing to exercise in the built app. Green: 1260 pytest (5 opt-in skips), 170 desktop, 274 renderer, 11 scripts. | P3-16 is topmost (preemption flake), then `P3-01`'s absolute `move_to` |
| 2026-09-24 | claude-code | P3-06 | Kill switch, security-gated. **Picked P3-06, not P3-16:** the last handoff called P3-16 topmost, but P3-06 sits above it in the table and both its blockers were DONE. **Measured before designing.** `taskkill /PID … /T /F` on a two-process tree took **~300 ms**, over the 200 ms budget by itself, so the tree walk cannot be the fast path. The handshake already reports the core's own PID, so MAIN can `TerminateProcess` the core directly. That matters because in development the core is MAIN's *grandchild*. **Design:** ask first, then terminate. MAIN sends `POST /v1/kill` with a 100 ms deadline. A healthy core engages a sticky `KillSwitch` (`actuation/killswitch.py`): every attached `InputController` aborts and has its keys and buttons released, then the core answers and **stays up**, keeping its journal for the Undo offer. Anything else, and the supervisor terminates both PIDs and starts a fresh core with a clean budget. The binding lives in MAIN (`hotkeys.ts`, `%LOCALAPPDATA%\Aegis\hotkeys.json`): at least two modifiers, and a rebind registers the new shortcut before releasing the old. If the switch cannot be armed, `get()` refuses. The tray item is the same trigger. **Found while testing:** a `key_down()` whose event lands just after the sweep has no later checkpoint, so it would leave a key held after a kill that reported success. `InputController` now re-checks after each primitive, and a test drives exactly that interleaving. **Verified:** real core acknowledged in 2.8–7.8 ms. The real core, suspended with `NtSuspendProcess` (REVIEW §5's SIGSTOP), was dead **102 ms** after the press, its launcher too; a fresh core came back and nothing was left after stop. The live vitest against a wedged two-process fake core asserts < 200 ms. In the built app, an injected Ctrl+Alt+Shift+Q reached the core, and closing the app took the core with it (launching Electron from the VS Code terminal needs `ELECTRON_RUN_AS_NODE` unset). Mutation checks: dropping the handshake-PID kill, the `engaged` check, the two-modifier rule, the release of the old binding, either `InputController` check, or making the sweep stop at the first failure each turns a test red. The last one survived at first because weak-set order is arbitrary; the test now uses two broken controllers. 5 Decision Log rows; `ARCHITECTURE.md` § 4, § 8.3 and § 9.1 updated; notes carried to P3-14, P3-15 and P5-08 (the Job Object requirement). Green: 1274 pytest (5 opt-in skips), 223 desktop, 274 renderer, 11 scripts; build, typecheck and lint clean. The two P3-16 preemption tests passed in this full run, so that flake is still unexplained. | P3-07 is topmost; P3-15 and P3-16 are unblocked |
| 2026-09-25 | claude-code | P3-07 | Watchdog, left in **REVIEW**: everything is done except the one hand run of the built app, because a running Aegis instance held the single-instance lock all session and I did not close the owner's app. **Design question:** how does MAIN know "a task is running" when the core is the thing that has stopped answering? From the `task.status` events it already forwards. What the core said before the hang is still true after it, while a flag on `/health` goes stale at exactly that moment. New `main/task-activity.ts` counts `RUNNING` only and clears on a stream `reset`. `TaskState` is now a vocabulary in `server/schemas.py` (and `TASK_STATES` in TS), and a test holds it equal to `tasks.status`'s CHECK. `main/watchdog.ts`: while running, `GET /health` every 1 s with a 1 s deadline. Two misses in a row against the **same** gateway call `supervisor.terminate('watchdog')`, which is the kill switch's `TerminateProcess` path. `terminate()` now takes a cause: a watchdog kill **counts** against the 3-in-60 s budget and a kill-switch press does not. A round whose deadline fired > 250 ms late means MAIN or the machine slept, so it resets the count. A terminate that throws does not stop the watchdog. **Live**, with the real supervisor and handshake at real settings: a core wedged mid-task was dead **~2.5 s** later (3 runs: 2502–2513 ms), the launcher too. A wedged core with no task running is left alone. 13 mutations, all red; one survived at first because a test boundary sat 1 ms early, and the test was tightened. **Not in this task:** nothing publishes `task.status` until P4, so the watchdog is armed but idle (the P4-02 row now says so). Also, `a109aeb "next phase"` was committed from my in-progress tree mid-session. I checked that it holds no mutated code, and left it alone. It has a ruff import-order error in `test_db.py`, fixed in the P3-07 commit. **End-of-session run:** 1 pytest failure, `test_downscaling_and_encoding_a_busy_desktop_fit_the_budget` at ~305 ms. It fails the same way (332 ms) on the untouched `6aa7dfe`, so it is machine load and not this change. New task **P2-14** covers it. Otherwise green: 1282 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. 2 Decision Log rows; `ARCHITECTURE.md` § 4, § 8.3 and § 9.2 updated. | Finish P3-07 (launch the built app once, then DONE), then P3-08; P3-15, P3-16 and P2-14 are unblocked |
| 2026-09-25 | claude-code | P2-14 | Encode-budget gate made load-robust; only tests changed. P2-14 was the topmost TODO, above P3-08. P3-07 stays in REVIEW until someone runs the built app by hand. The start gate was red on exactly this test (318 ms). **Measured first.** The laptop was **on battery** (Balanced plan) with other programs busy. Outside pytest the same frame took ~92 ms, and the test itself passed minutes later, so the cause was machine state. I then put 0/4/8/12 spinning processes on the 8 logical CPUs. The encode ran 96 → 419 ms wall time. `thread_time` is useless here: it ticks in 15.6 ms steps and still grew to 156 ms. sha256 did not slow down (SHA hardware); **zlib did, in step with the encode.** **Decided:** a ratio gate plus structural pins. The busy-desktop encode is timed alternately with a fixed 1 MB zlib run, and the test takes the median of the per-pair ratios, ceiling **3.2**. The real code measured 1.25–1.62 across all loads; WebP method 4 measured 3.69–5.37. New `test_the_encoder_runs_libwebps_fastest_method` spies on `save()` and pins `method=0` with no timing. The downscale was already pinned by the output-size asserts. The absolute 150 ms stays in the `AEGIS_PERF` live benchmark. Mutations: method 4 (ratio test and spy) and a skipped downscale (8 tests) are all red. Residual risk, written down: a CPU whose zlib-to-libwebp speed ratio differs a lot from this one. One Decision Log row, superseding half of P2-10's. There is no app surface, so nothing to run by hand. Green: 1284 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-07 needs its hand run; then P3-08; P3-15 and P3-16 are unblocked |
| 2026-09-25 | claude-code | P3-08 | Guardian core, security-gated because it is the Guardian. New `guardian/risk.py`, `rules.py`, `rules.yaml` and `policy.py`; `Autonomy` and `Decision` added to the wire vocabulary; `pyyaml` added as a runtime dependency. **Design calls, 3 Decision Log rows.** (1) `rules.yaml` is **digest-pinned** in bytecode, so invariant 5's "compiled in" holds while the rules stay reviewable YAML, and an edited or missing file stops the Guardian from starting. (2) Tools declare their **targets**, and matching runs on **canonical** forms; a target with no canonical form is denied. **Measured before writing the normaliser:** `GetFullPathNameW` strips trailing dots, but not in front of a stream name or in a `dir. ` component. The tests use a real junction (`_winapi.CreateJunction`), the real `C:\PROGRA~1`, and file-level rules for streams and dots. A folder-level rule would match those spellings anyway, which is why 2 mutations survived at first; the file-level tests closed that gap. **Found while writing it:** `realpath` on a model-supplied `\host\share` would open an SMB session, a hang plus an NTLM-hash leak, so UNC paths are handled lexically, and a test proves `realpath` is never called for them. (3) `§ 8.1`'s user always-allow step is **left to P3-11**. Under `§ 10` the only confirm left for it to relax is DANGEROUS, which it may not relax, so as sketched it was dead code. P3-11's row now carries the constraint. `rules.yaml` ships with an **empty** FORBIDDEN list; the entries are P3-09's, and no task can run before P3-09 because P4-02 waits on all of P3. Notes also went to P3-09, P3-10, P4-01 (sixth required member, `targets()`) and P7-01 (bundle `rules.yaml`). 213 guardian tests, including an exhaustive 1,280-case sweep for "DANGEROUS is never allowed", "observe allows only SAFE" and "signals never make a call easier". 23 mutations, all red. No app surface, so there is nothing to run by hand. Green: 1499 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-07 still needs its hand run. Next: P3-09 (FORBIDDEN entries, security-gated); P3-10, P3-11, P3-15 and P3-16 are also unblocked |
| 2026-09-25 | claude-code | P3-09 | The FORBIDDEN list, security-gated. **Start gate:** one failure, `test_a_live_hit_test_lands_on_the_button_and_climbs_to_its_window`. Its hit landed on a different top-level window, most likely something the user opened over the test form. It passed 3/3 on rerun and in the end-of-session run. It is P2 code and was left alone, but it joins P3-16 as a live desktop test that someone using the machine can disturb. **The list:** 28 entries and 100 patterns in `rules.yaml`, covering `§ 8.1`'s examples plus the classes they stand for. For instance, the Startup folders do what the Run keys do, and a browser's `Local State` holds the key that decrypts its saved passwords. Encoded PowerShell is forbidden because the Guardian cannot read it. System folders are write-only, credentials are any-access, and all of Aegis' own data is any-access. **Loader additions:** `%AEGIS_DATA%` / `%AEGIS_INSTALL%` are computed by the core (the installer lets the user choose the directory, and an env var could be pointed anywhere). Command rules also match a de-obfuscated form (`for^mat`, `"vss"admin`). **Tests:** `test_forbidden.py` gives every entry and **every pattern** a denied example, run through the real Guardian in its most permissive context (`trusted`, with everything in scope). It also has 30 ordinary operations next to those places that must stay allowed, a check that no route serves the rules, and a check that nothing outside `guardian/` imports the loader. The per-pattern coverage check caught a real gap on its first run: an LSASS-dump pattern that no example reached. 9 mutations to the list and loader are all red; each YAML mutation re-pinned the digest, so the list's own tests are what caught it. Two Decision Log rows, and `ARCHITECTURE.md § 8.1` updated. Notes carried to P3-10 and P5-09. No app surface, so there is nothing to run by hand. Green: 1650 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-07 still needs its hand run. Next: P3-10 (scope model, security-gated); P3-11, P3-15 and P3-16 are unblocked too |
| 2026-09-25 | claude-code | P3-10 | Scope model, security-gated. **Probed first, which found four ways past P3-08's normaliser:** `\.\C:\Windows\…` came back as a device string no FORBIDDEN rule matches; `\localhost\C$` (also `127.1`, `2130706433` and this PC's own name) reached the local disk; `C:\x\NUL` became the device `\.\nul`; and a symlink to a share, which Developer Mode lets anyone create, made `realpath` try SMB for 42 s. All four are now refused. Network links are found by `lstat` + `readlink` and never opened. `subst` drives resolve, and mapped network drives are handled lexically. **Model:** `guardian/scope.py` (name, canonical folders, app exe names; no drive or share roots, no FORBIDDEN folders, nesting collapsed, capped at 32), the `scopes` table (m0003) with `storage/scopes.py`, and `load_scope()`, which re-validates on load so a stored row can only come back narrower. `Guardian.check_target()` is the re-check at the moment of acting, and a test swaps a folder for a junction after approval. **Split out:** P3-17 holds the routes, the user-picked-folder trust path and the UI. The renderer's core passthrough is generic, so a `POST /scopes` taking any folder would let a compromised renderer widen reach; closing that needs a MAIN and preload change. 92 new tests, with real junctions, symlinks and `subst`. 19 mutations, all red: 3 survived at first, all overlapping defensive layers; one was removed and the other two got isolating tests. 2 Decision Log rows; `ARCHITECTURE.md` § 4, § 7 and § 8.2 updated. The owner asked mid-task for P3-01 next. No app surface, so there is nothing to run by hand. Green: 1742 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-01 (owner's request), then P3-11 / P3-17; P3-07 still needs its hand run |
| 2026-09-25 | claude-code | P3-01 | Finished the SendInput wrapper at the owner's request; it had been WIP since P3-04 landed half of it. **Measured first:** `MapVirtualKeyW(VK_TO_VSC_EX)` reports Left as `0x4B` (numpad 4's scan code) and does not mark the navigation cluster `E0`; `VkKeyScanW` gives `?` as Shift + `0xBF` here. Keys previously went out with `wScan = 0` and a 4-key extended set, so Left and Delete arrived as their numpad twins in apps that read scan codes. **Now:** keys carry the real scan code, and the extended flag comes from `E0` or Microsoft's documented list. New `actuation/keys.py` holds the named-key table and `parse_combo()`, which looks a single character up on the user's own layout and refuses one the layout cannot type. `move_to` / `click_at` / `drag_to` work in P2-01's physical pixels via `to_absolute()` with `VIRTUALDESK`, and each re-checks the display layout before moving. A drag converts its whole path before the button goes down, so one that would cross a dead zone never starts. Also `press_keys()`. The backend's locked surface gained `mouse_move_absolute`, and the signature test checks it is tagged. **Live:** `move_to` through the real controller put the real cursor on every probed pixel of this desk and put it back afterwards; that is the by-hand exercise, since there is no app surface. 58 new tests; 9 mutations, all red. One Decision Log row; `ARCHITECTURE.md` § 4 and § 6.4 updated. Green: 1800 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-03 (human-like motion) is now unblocked; P3-11 / P3-17; P3-07 still needs its hand run |
| 2026-09-25 | claude-code | P3-03 | Human-like motion; it was the topmost TODO once P3-01 was DONE. New `actuation/motion.py`: `plan_path()` gives a cubic Bézier from the current cursor, bowing at most 15 % of its length, with smoothstep easing and ±1 px jitter on the points in between; the path always ends exactly on the target. It runs at ~120 moves a second for a Fitts'-law time clamped to 0.08–0.45 s. `InputController(motion=HUMAN)` uses it for `move_to` / `click_at` / `drag_to`, with a 40–120 ms dwell before a press. The start point is read with `GetCursorPos` (added to `actuation/win32.py`); with no known cursor it jumps. A curve that bows off every monitor falls back to a straight line. If even the straight line crosses a dead zone, a move jumps and a drag is refused before anything moves. The pause between steps waits on the preemption signal, so a slow move still stops the instant the human touches the mouse; a test proves it. Opt-in, so every existing caller stays deterministic; the P4-02 row now says to pass `HUMAN`. 58 new tests, and live on this desk the cursor lands exactly on target within the time bounds. 11 mutations all red: 3 survived the first run because their tests were too weak (easing, step cap, preemption during a pause), and those tests were strengthened. One Decision Log row; `ARCHITECTURE.md` § 4 and § 6.4 updated. Green: 1986 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-11 (approvals) is topmost; P3-15, P3-16 and P3-17 are unblocked; P3-07 still needs its hand run |
| 2026-09-25 | claude-code | P3-11 | Approvals, security-gated because it changes the Guardian. **`ApprovalBroker`** (`guardian/approvals.py`): `request()` publishes `approval.requested` and waits. The deadline is held **in the core**: 30 s for a DANGEROUS effective tier, 60 s otherwise, and it **denies**. The kill switch (`POST /v1/kill` now calls `deny_all`), a cancelled wait and shutdown all deny what is pending. Each approval gets exactly one answer and one `approval.resolved`; a late click is a 404. **Always-allow rules** (`guardian/allow_rules.py`, m0004 `allow_rules`) come in three kinds, exact / tool_in_folder / tool_for_task, and are made from the *pending call*; the request names only the kind, so the renderer never supplies a path. They are `evaluate()` step 4, after the signals: trusted only, SAFE/CAUTION only, never over an echo confirm or any deny. A folder rule needs *every* target to be a path inside the folder. **Routes:** `POST /approvals/{id}`, plus `GET` and `DELETE /rules` so a rule can be revoked. The `approvals` table needs a `step_id`, so P4-02 records the `Resolution`; its row now says how. **Found by tests:** SQLite passes a `CHECK` that evaluates to NULL, so the new table accepted a rule with nothing to match on; fixed with explicit `IS NOT NULL`. A mutation then showed a command whose text starts with a folder path could have satisfied a folder rule, so a test pins it. Also found: `main()` leaked the rule store's connection when the server never ran; now closed. New task **P3-18** (Rules screen, `UI.md § 8.3`, which no task owned). 44 new tests; 13 mutations, all red. 2 Decision Log rows; `ARCHITECTURE.md` § 4, § 7, § 8.1, § 9.1 and § 9.2 updated. No app surface, so there is nothing to run by hand. Green: 2033 pytest (5 opt-in skips), 265 desktop, 274 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-12 (ApprovalDialog) is topmost; P3-15 to P3-18 are unblocked; P3-07 still needs its hand run |
| 2026-09-25 | claude-code | P3-12 | The approval dialog. `components/ApprovalDialog.tsx`, mounted over the main window in `AppShell`. It is a **pure function of the stream**: `lib/approvals.ts` validates every `approval.requested` field and removes a question on `approval.resolved`, so the dialog closes exactly when the core's question does. It shows one question at a time, oldest first. `stores/approval.ts` holds only the dialog's own state (the guard, the answer in flight, the notice), so stories reach every state without faking one. **Rules kept:** Deny is focused and **never disabled**; the 200 ms guard covers only the Allow buttons and re-arms for each question; Esc denies; Tab and Shift+Tab are trapped and focus is returned on close. The countdown is display only, computed from the event's own timestamp. *Allow always* shows only the kinds the core offered. The reversibility banner is red unless the event says `reversible: true`; that field was added to P3-11's event, since it carried none and an unknown must never read as "recoverable". Nothing animates (the buttons override `Button`'s transition), and the overlay is `app-no-drag` so the titlebar's drag region cannot steal a click. **Caught while writing:** a React 18 `ref` on `Button` would have been dropped silently, so Deny would never have got focus; it is now found by a data attribute. Failed answers use an assertive live region. Tests: stories for Default, Guarded, Loading, Error, ReadOutsideScope and LongestContent (Empty is declared not applicable: nothing renders), plus 30 behaviour tests; 12 mutations all red. There is no core producer until P4, so the built app cannot raise a dialog yet; the stories render in the test harness and `vite build` is in the gate. New task **P3-19**: the dialog's own always-on-top window (`UI.md § 3`), which is MAIN work. One Decision Log row; `UI.md § 5` and `ARCHITECTURE.md § 9.2` updated. Green: 2033 pytest (5 opt-in skips), 265 desktop, 318 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-13 (OverlayHUD) is topmost; P3-15 to P3-19 are unblocked; P3-07 still needs its hand run |
| 2026-09-25 | claude-code | P3-13 | OverlayHUD, security-gated because it adds a preload. **Spec conflict resolved:** `UI.md § 6` wants a HUD that "never covers the cursor" *and* has clickable buttons. Invariant 1 settles it: while a task runs, every cursor movement is the agent's, so the HUD is click-through and dodges to the other edge within 24 px of the cursor; paused, waiting or idle, it takes clicks and holds still. **MAIN:** `overlay.ts` makes a frameless, transparent, always-on-top window that is never focusable and is shown inactive, with `setContentProtection` excluding it from capture; `overlay-policy.ts` is pure placement. **Its own preload**, `preload/hud.cts`: `aegisHud` has subscribe / stop (the kill switch) / showMain / deny, and **no allow**. Its channels answer only the HUD's `webContents`, through the reused `isSenderTrusted()`. The HUD page gets a stream reset and replay on load, like the main window. **Renderer:** a second Vite page (`hud.html`, `src/hud.tsx`), pure `hud-state.ts` (offline > approval > task state, amber / red tones), and `OverlayHUD` with Deny and Review in the approval state, ■ Stop and ⌃ Open. **Hand run on the built code**, driven by a script against the real `overlay.js`: it opened top-centre, non-focusable and on top; with a task "running" and the cursor put on it, it moved from y 12 to 876; idle, it stayed. Its page renders as designed (seen via `capturePage`), and **the core's own capture of its rectangle showed the IDE behind it**, not the HUD and not a black box. A live Python test proves an excluded window is not captured, and is once the affinity is cleared. 10 mutations, all red. Not built yet: automatic showing (P3-14) and the recent-steps strip (P4-11). 2 Decision Log rows; `UI.md § 6` and `ARCHITECTURE.md` § 4 and § 9.3 updated. Green: 2034 pytest (5 opt-in skips), 291 desktop, 349 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-15 (MAIN's own modifier release) is topmost; P3-14, P3-16 to P3-19 are unblocked; P3-07 still needs its hand run (Aegis is closed now, so it can be done) |
| 2026-09-26 | claude-code | P3-15 | MAIN's own modifier release (`RECOVERY.md § 4`). **Measured first:** a spawned helper cannot meet the kill switch's 200 ms (venv Python ~95 ms to start, PowerShell ~257 ms, and the hung-core path already spends ~100 ms), so `koffi` (prebuilt N-API FFI, MIT) calls `GetAsyncKeyState` and `SendInput` from MAIN in well under a microsecond; it loaded in Node and in Electron 32.3.3. `main/win-input.ts` sends `KEYUP`s built as the core builds them (scan code, extended flag, `AEGIS_SIGNATURE`, with a parity test against `signature.py`). `main/modifier-release.ts` releases all eight modifiers that are down and never stops part-way. **Wired:** straight *after* `supervisor.terminate()`'s kill, because a hung core's input thread could press again, and once more on that process's exit; this covers both the kill switch and the watchdog. It also runs first thing when a core dies on its own during a task (not when idle, since those keys are the person's), and after a kill ack with `release_failures > 0`. A supervisor test caught the exit listener being attached after `kill()`; it is now attached before. **Live:** a genuinely held Shift and right Ctrl come up in < 20 ms. 11 mutations all red. **Incident, REMEMBER § 9:** during the mutation run, left Shift and right Ctrl were left held on the developer's machine for a few minutes. The key-down mutation broke the test's own cleanup, which used the code under test. The keys were released with the real code, and the test's cleanup now uses an independent `SendInput`; re-running that mutation leaves nothing held. The release cannot be triggered in the built app until there are tasks (P4). One Decision Log row; `ARCHITECTURE.md § 8.3` and `RECOVERY.md § 4` updated; P7-02 must unpack `koffi` from the asar. Green: 2034 pytest (5 opt-in skips), 307 desktop, 349 renderer, scripts; build, typecheck, lint, ruff and mypy clean. | P3-16 (preemption flake) is topmost; P3-14, P3-17 to P3-19 are unblocked; P3-07 still needs its hand run |
