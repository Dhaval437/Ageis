/**
 * IPC handlers for the preload bridge.
 *
 * Every channel is explicitly registered here. There is no generic `invoke`
 * passthrough — see PROGRESS.md P0-04.
 *
 * Scaffold only — implemented in P0-04.
 */

export {};
